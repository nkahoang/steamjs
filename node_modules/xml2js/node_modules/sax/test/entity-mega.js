var sax = require('../'***REMOVED***;
var xml = '<r>';
var text = '';
for (var i in sax.ENTITIES***REMOVED*** {
  xml += '&' + i + ';'
  text += sax.ENTITIES[i]
}
xml += '</r>'
require(__dirname***REMOVED***.test({
  xml: xml,
  expect: [
    ['opentag', {'name':'R', attributes:{}, isSelfClosing: false}],
    ['text', text],
    ['closetag', 'R']
  ]
}***REMOVED***;
