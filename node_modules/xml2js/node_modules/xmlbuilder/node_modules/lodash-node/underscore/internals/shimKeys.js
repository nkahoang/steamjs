/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize underscore exports="node" -o ./underscore/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var objectTypes = require('./objectTypes'***REMOVED***;

/** Used for native method references */
var objectProto = Object.prototype;

/** Native method shortcuts */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * A fallback implementation of `Object.keys` which produces an array of the
 * given object's own enumerable property names.
 *
 * @private
 * @type Function
 * @param {Object} object The object to inspect.
 * @returns {Array} Returns an array of property names.
 */
var shimKeys = function(object***REMOVED*** {
  var index, iterable = object, result = [];
  if (!iterable***REMOVED*** return result;
  if (!(objectTypes[typeof object]***REMOVED******REMOVED*** return result;
    for (index in iterable***REMOVED*** {
      if (hasOwnProperty.call(iterable, index***REMOVED******REMOVED*** {
        result.push(index***REMOVED***;
    ***REMOVED***
  ***REMOVED***
  return result
};

module.exports = shimKeys;
