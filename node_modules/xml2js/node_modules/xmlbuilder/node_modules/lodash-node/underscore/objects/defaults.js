/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize underscore exports="node" -o ./underscore/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var keys = require('./keys'***REMOVED***,
    objectTypes = require('../internals/objectTypes'***REMOVED***;

/**
 * Assigns own enumerable properties of source object(s***REMOVED*** to the destination
 * object for all destination properties that resolve to `undefined`. Once a
 * property is set, additional defaults of the same property will be ignored.
 *
 * @static
 * @memberOf _
 * @type Function
 * @category Objects
 * @param {Object} object The destination object.
 * @param {...Object} [source] The source objects.
 * @param- {Object} [guard] Allows working with `_.reduce` without using its
 *  `key` and `object` arguments as sources.
 * @returns {Object} Returns the destination object.
 * @example
 *
 * var object = { 'name': 'barney' };
 * _.defaults(object, { 'name': 'fred', 'employer': 'slate' }***REMOVED***;
 * // => { 'name': 'barney', 'employer': 'slate' }
 */
function defaults(object***REMOVED*** {
  if (!object***REMOVED*** {
    return object;
***REMOVED***
  for (var argsIndex = 1, argsLength = arguments.length; argsIndex < argsLength; argsIndex++***REMOVED*** {
    var iterable = arguments[argsIndex];
    if (iterable***REMOVED*** {
      for (var key in iterable***REMOVED*** {
        if (typeof object[key] == 'undefined'***REMOVED*** {
          object[key] = iterable[key];
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
***REMOVED***
  return object;
}

module.exports = defaults;
