/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize modern exports="node" -o ./modern/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var isArguments = require('../objects/isArguments'***REMOVED***,
    isArray = require('../objects/isArray'***REMOVED***;

/**
 * The base implementation of `_.flatten` without support for callback
 * shorthands or `thisArg` binding.
 *
 * @private
 * @param {Array} array The array to flatten.
 * @param {boolean} [isShallow=false] A flag to restrict flattening to a single level.
 * @param {boolean} [isStrict=false] A flag to restrict flattening to arrays and `arguments` objects.
 * @param {number} [fromIndex=0] The index to start from.
 * @returns {Array} Returns a new flattened array.
 */
function baseFlatten(array, isShallow, isStrict, fromIndex***REMOVED*** {
  var index = (fromIndex || 0***REMOVED*** - 1,
      length = array ? array.length : 0,
      result = [];

  while (++index < length***REMOVED*** {
    var value = array[index];

    if (value && typeof value == 'object' && typeof value.length == 'number'
        && (isArray(value***REMOVED*** || isArguments(value***REMOVED******REMOVED******REMOVED*** {
      // recursively flatten arrays (susceptible to call stack limits***REMOVED***
      if (!isShallow***REMOVED*** {
        value = baseFlatten(value, isShallow, isStrict***REMOVED***;
    ***REMOVED***
      var valIndex = -1,
          valLength = value.length,
          resIndex = result.length;

      result.length += valLength;
      while (++valIndex < valLength***REMOVED*** {
        result[resIndex++] = value[valIndex];
    ***REMOVED***
  ***REMOVED*** else if (!isStrict***REMOVED*** {
      result.push(value***REMOVED***;
  ***REMOVED***
***REMOVED***
  return result;
}

module.exports = baseFlatten;
