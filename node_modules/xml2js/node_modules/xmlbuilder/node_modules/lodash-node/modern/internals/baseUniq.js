/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize modern exports="node" -o ./modern/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var baseIndexOf = require('./baseIndexOf'***REMOVED***,
    cacheIndexOf = require('./cacheIndexOf'***REMOVED***,
    createCache = require('./createCache'***REMOVED***,
    getArray = require('./getArray'***REMOVED***,
    largeArraySize = require('./largeArraySize'***REMOVED***,
    releaseArray = require('./releaseArray'***REMOVED***,
    releaseObject = require('./releaseObject'***REMOVED***;

/**
 * The base implementation of `_.uniq` without support for callback shorthands
 * or `thisArg` binding.
 *
 * @private
 * @param {Array} array The array to process.
 * @param {boolean} [isSorted=false] A flag to indicate that `array` is sorted.
 * @param {Function} [callback] The function called per iteration.
 * @returns {Array} Returns a duplicate-value-free array.
 */
function baseUniq(array, isSorted, callback***REMOVED*** {
  var index = -1,
      indexOf = baseIndexOf,
      length = array ? array.length : 0,
      result = [];

  var isLarge = !isSorted && length >= largeArraySize,
      seen = (callback || isLarge***REMOVED*** ? getArray(***REMOVED*** : result;

  if (isLarge***REMOVED*** {
    var cache = createCache(seen***REMOVED***;
    indexOf = cacheIndexOf;
    seen = cache;
***REMOVED***
  while (++index < length***REMOVED*** {
    var value = array[index],
        computed = callback ? callback(value, index, array***REMOVED*** : value;

    if (isSorted
          ? !index || seen[seen.length - 1] !== computed
          : indexOf(seen, computed***REMOVED*** < 0
        ***REMOVED*** {
      if (callback || isLarge***REMOVED*** {
        seen.push(computed***REMOVED***;
    ***REMOVED***
      result.push(value***REMOVED***;
  ***REMOVED***
***REMOVED***
  if (isLarge***REMOVED*** {
    releaseArray(seen.array***REMOVED***;
    releaseObject(seen***REMOVED***;
***REMOVED*** else if (callback***REMOVED*** {
    releaseArray(seen***REMOVED***;
***REMOVED***
  return result;
}

module.exports = baseUniq;
