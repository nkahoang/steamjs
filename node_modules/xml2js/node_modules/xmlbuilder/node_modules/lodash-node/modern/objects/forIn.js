/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize modern exports="node" -o ./modern/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var baseCreateCallback = require('../internals/baseCreateCallback'***REMOVED***,
    objectTypes = require('../internals/objectTypes'***REMOVED***;

/**
 * Iterates over own and inherited enumerable properties of an object,
 * executing the callback for each property. The callback is bound to `thisArg`
 * and invoked with three arguments; (value, key, object***REMOVED***. Callbacks may exit
 * iteration early by explicitly returning `false`.
 *
 * @static
 * @memberOf _
 * @type Function
 * @category Objects
 * @param {Object} object The object to iterate over.
 * @param {Function} [callback=identity] The function called per iteration.
 * @param {*} [thisArg] The `this` binding of `callback`.
 * @returns {Object} Returns `object`.
 * @example
 *
 * function Shape(***REMOVED*** {
 *   this.x = 0;
 *   this.y = 0;
 * }
 *
 * Shape.prototype.move = function(x, y***REMOVED*** {
 *   this.x += x;
 *   this.y += y;
 * };
 *
 * _.forIn(new Shape, function(value, key***REMOVED*** {
 *   console.log(key***REMOVED***;
 * }***REMOVED***;
 * // => logs 'x', 'y', and 'move' (property order is not guaranteed across environments***REMOVED***
 */
var forIn = function(collection, callback, thisArg***REMOVED*** {
  var index, iterable = collection, result = iterable;
  if (!iterable***REMOVED*** return result;
  if (!objectTypes[typeof iterable]***REMOVED*** return result;
  callback = callback && typeof thisArg == 'undefined' ? callback : baseCreateCallback(callback, thisArg, 3***REMOVED***;
    for (index in iterable***REMOVED*** {
      if (callback(iterable[index], index, collection***REMOVED*** === false***REMOVED*** return result;
  ***REMOVED***
  return result
};

module.exports = forIn;
