/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize modern exports="node" -o ./modern/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var baseRandom = require('../internals/baseRandom'***REMOVED***;

/* Native method shortcuts for methods with the same name as other `lodash` methods */
var nativeMin = Math.min,
    nativeRandom = Math.random;

/**
 * Produces a random number between `min` and `max` (inclusive***REMOVED***. If only one
 * argument is provided a number between `0` and the given number will be
 * returned. If `floating` is truey or either `min` or `max` are floats a
 * floating-point number will be returned instead of an integer.
 *
 * @static
 * @memberOf _
 * @category Utilities
 * @param {number} [min=0] The minimum possible value.
 * @param {number} [max=1] The maximum possible value.
 * @param {boolean} [floating=false] Specify returning a floating-point number.
 * @returns {number} Returns a random number.
 * @example
 *
 * _.random(0, 5***REMOVED***;
 * // => an integer between 0 and 5
 *
 * _.random(5***REMOVED***;
 * // => also an integer between 0 and 5
 *
 * _.random(5, true***REMOVED***;
 * // => a floating-point number between 0 and 5
 *
 * _.random(1.2, 5.2***REMOVED***;
 * // => a floating-point number between 1.2 and 5.2
 */
function random(min, max, floating***REMOVED*** {
  var noMin = min == null,
      noMax = max == null;

  if (floating == null***REMOVED*** {
    if (typeof min == 'boolean' && noMax***REMOVED*** {
      floating = min;
      min = 1;
  ***REMOVED***
    else if (!noMax && typeof max == 'boolean'***REMOVED*** {
      floating = max;
      noMax = true;
  ***REMOVED***
***REMOVED***
  if (noMin && noMax***REMOVED*** {
    max = 1;
***REMOVED***
  min = +min || 0;
  if (noMax***REMOVED*** {
    max = min;
    min = 0;
***REMOVED*** else {
    max = +max || 0;
***REMOVED***
  if (floating || min % 1 || max % 1***REMOVED*** {
    var rand = nativeRandom(***REMOVED***;
    return nativeMin(min + (rand * (max - min + parseFloat('1e-' + ((rand +''***REMOVED***.length - 1***REMOVED******REMOVED******REMOVED******REMOVED***, max***REMOVED***;
***REMOVED***
  return baseRandom(min, max***REMOVED***;
}

module.exports = random;
