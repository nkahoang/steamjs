/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize modern exports="node" -o ./modern/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var createCallback = require('../functions/createCallback'***REMOVED***,
    forEachRight = require('./forEachRight'***REMOVED***;

/**
 * This method is like `_.find` except that it iterates over elements
 * of a `collection` from right to left.
 *
 * @static
 * @memberOf _
 * @category Collections
 * @param {Array|Object|string} collection The collection to iterate over.
 * @param {Function|Object|string} [callback=identity] The function called
 *  per iteration. If a property name or object is provided it will be used
 *  to create a "_.pluck" or "_.where" style callback, respectively.
 * @param {*} [thisArg] The `this` binding of `callback`.
 * @returns {*} Returns the found element, else `undefined`.
 * @example
 *
 * _.findLast([1, 2, 3, 4], function(num***REMOVED*** {
 *   return num % 2 == 1;
 * }***REMOVED***;
 * // => 3
 */
function findLast(collection, callback, thisArg***REMOVED*** {
  var result;
  callback = createCallback(callback, thisArg, 3***REMOVED***;
  forEachRight(collection, function(value, index, collection***REMOVED*** {
    if (callback(value, index, collection***REMOVED******REMOVED*** {
      result = value;
      return false;
  ***REMOVED***
***REMOVED******REMOVED***;
  return result;
}

module.exports = findLast;
