/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize exports="node" -o ./compat/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var cachePush = require('./cachePush'***REMOVED***,
    getObject = require('./getObject'***REMOVED***,
    releaseObject = require('./releaseObject'***REMOVED***;

/**
 * Creates a cache object to optimize linear searches of large arrays.
 *
 * @private
 * @param {Array} [array=[]] The array to search.
 * @returns {null|Object} Returns the cache object or `null` if caching should not be used.
 */
function createCache(array***REMOVED*** {
  var index = -1,
      length = array.length,
      first = array[0],
      mid = array[(length / 2***REMOVED*** | 0],
      last = array[length - 1];

  if (first && typeof first == 'object' &&
      mid && typeof mid == 'object' && last && typeof last == 'object'***REMOVED*** {
    return false;
***REMOVED***
  var cache = getObject(***REMOVED***;
  cache['false'] = cache['null'] = cache['true'] = cache['undefined'] = false;

  var result = getObject(***REMOVED***;
  result.array = array;
  result.cache = cache;
  result.push = cachePush;

  while (++index < length***REMOVED*** {
    result.push(array[index]***REMOVED***;
***REMOVED***
  return result;
}

module.exports = createCache;
