/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize exports="node" -o ./compat/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var keyPrefix = require('./keyPrefix'***REMOVED***;

/**
 * Adds a given value to the corresponding cache object.
 *
 * @private
 * @param {*} value The value to add to the cache.
 */
function cachePush(value***REMOVED*** {
  var cache = this.cache,
      type = typeof value;

  if (type == 'boolean' || value == null***REMOVED*** {
    cache[value] = true;
***REMOVED*** else {
    if (type != 'number' && type != 'string'***REMOVED*** {
      type = 'object';
  ***REMOVED***
    var key = type == 'number' ? value : keyPrefix + value,
        typeCache = cache[type] || (cache[type] = {}***REMOVED***;

    if (type == 'object'***REMOVED*** {
      (typeCache[key] || (typeCache[key] = []***REMOVED******REMOVED***.push(value***REMOVED***;
  ***REMOVED*** else {
      typeCache[key] = true;
  ***REMOVED***
***REMOVED***
}

module.exports = cachePush;
