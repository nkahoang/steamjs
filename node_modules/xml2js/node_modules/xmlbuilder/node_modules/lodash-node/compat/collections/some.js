/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize exports="node" -o ./compat/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var baseEach = require('../internals/baseEach'***REMOVED***,
    createCallback = require('../functions/createCallback'***REMOVED***,
    isArray = require('../objects/isArray'***REMOVED***;

/**
 * Checks if the callback returns a truey value for **any** element of a
 * collection. The function returns as soon as it finds a passing value and
 * does not iterate over the entire collection. The callback is bound to
 * `thisArg` and invoked with three arguments; (value, index|key, collection***REMOVED***.
 *
 * If a property name is provided for `callback` the created "_.pluck" style
 * callback will return the property value of the given element.
 *
 * If an object is provided for `callback` the created "_.where" style callback
 * will return `true` for elements that have the properties of the given object,
 * else `false`.
 *
 * @static
 * @memberOf _
 * @alias any
 * @category Collections
 * @param {Array|Object|string} collection The collection to iterate over.
 * @param {Function|Object|string} [callback=identity] The function called
 *  per iteration. If a property name or object is provided it will be used
 *  to create a "_.pluck" or "_.where" style callback, respectively.
 * @param {*} [thisArg] The `this` binding of `callback`.
 * @returns {boolean} Returns `true` if any element passed the callback check,
 *  else `false`.
 * @example
 *
 * _.some([null, 0, 'yes', false], Boolean***REMOVED***;
 * // => true
 *
 * var characters = [
 *   { 'name': 'barney', 'age': 36, 'blocked': false },
 *   { 'name': 'fred',   'age': 40, 'blocked': true }
 * ];
 *
 * // using "_.pluck" callback shorthand
 * _.some(characters, 'blocked'***REMOVED***;
 * // => true
 *
 * // using "_.where" callback shorthand
 * _.some(characters, { 'age': 1 }***REMOVED***;
 * // => false
 */
function some(collection, callback, thisArg***REMOVED*** {
  var result;
  callback = createCallback(callback, thisArg, 3***REMOVED***;

  if (isArray(collection***REMOVED******REMOVED*** {
    var index = -1,
        length = collection.length;

    while (++index < length***REMOVED*** {
      if ((result = callback(collection[index], index, collection***REMOVED******REMOVED******REMOVED*** {
        break;
    ***REMOVED***
  ***REMOVED***
***REMOVED*** else {
    baseEach(collection, function(value, index, collection***REMOVED*** {
      return !(result = callback(value, index, collection***REMOVED******REMOVED***;
  ***REMOVED******REMOVED***;
***REMOVED***
  return !!result;
}

module.exports = some;
