/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize exports="node" -o ./compat/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var baseIndexOf = require('../internals/baseIndexOf'***REMOVED***,
    cacheIndexOf = require('../internals/cacheIndexOf'***REMOVED***,
    createCache = require('../internals/createCache'***REMOVED***,
    getArray = require('../internals/getArray'***REMOVED***,
    isArguments = require('../objects/isArguments'***REMOVED***,
    isArray = require('../objects/isArray'***REMOVED***,
    largeArraySize = require('../internals/largeArraySize'***REMOVED***,
    releaseArray = require('../internals/releaseArray'***REMOVED***,
    releaseObject = require('../internals/releaseObject'***REMOVED***;

/**
 * Creates an array of unique values present in all provided arrays using
 * strict equality for comparisons, i.e. `===`.
 *
 * @static
 * @memberOf _
 * @category Arrays
 * @param {...Array} [array] The arrays to inspect.
 * @returns {Array} Returns an array of shared values.
 * @example
 *
 * _.intersection([1, 2, 3], [5, 2, 1, 4], [2, 1]***REMOVED***;
 * // => [1, 2]
 */
function intersection(***REMOVED*** {
  var args = [],
      argsIndex = -1,
      argsLength = arguments.length,
      caches = getArray(***REMOVED***,
      indexOf = baseIndexOf,
      trustIndexOf = indexOf === baseIndexOf,
      seen = getArray(***REMOVED***;

  while (++argsIndex < argsLength***REMOVED*** {
    var value = arguments[argsIndex];
    if (isArray(value***REMOVED*** || isArguments(value***REMOVED******REMOVED*** {
      args.push(value***REMOVED***;
      caches.push(trustIndexOf && value.length >= largeArraySize &&
        createCache(argsIndex ? args[argsIndex] : seen***REMOVED******REMOVED***;
  ***REMOVED***
***REMOVED***
  var array = args[0],
      index = -1,
      length = array ? array.length : 0,
      result = [];

  outer:
  while (++index < length***REMOVED*** {
    var cache = caches[0];
    value = array[index];

    if ((cache ? cacheIndexOf(cache, value***REMOVED*** : indexOf(seen, value***REMOVED******REMOVED*** < 0***REMOVED*** {
      argsIndex = argsLength;
      (cache || seen***REMOVED***.push(value***REMOVED***;
      while (--argsIndex***REMOVED*** {
        cache = caches[argsIndex];
        if ((cache ? cacheIndexOf(cache, value***REMOVED*** : indexOf(args[argsIndex], value***REMOVED******REMOVED*** < 0***REMOVED*** {
          continue outer;
      ***REMOVED***
    ***REMOVED***
      result.push(value***REMOVED***;
  ***REMOVED***
***REMOVED***
  while (argsLength--***REMOVED*** {
    cache = caches[argsLength];
    if (cache***REMOVED*** {
      releaseObject(cache***REMOVED***;
  ***REMOVED***
***REMOVED***
  releaseArray(caches***REMOVED***;
  releaseArray(seen***REMOVED***;
  return result;
}

module.exports = intersection;
