/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize exports="node" -o ./compat/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var baseClone = require('../internals/baseClone'***REMOVED***,
    baseCreateCallback = require('../internals/baseCreateCallback'***REMOVED***;

/**
 * Creates a clone of `value`. If `isDeep` is `true` nested objects will also
 * be cloned, otherwise they will be assigned by reference. If a callback
 * is provided it will be executed to produce the cloned values. If the
 * callback returns `undefined` cloning will be handled by the method instead.
 * The callback is bound to `thisArg` and invoked with one argument; (value***REMOVED***.
 *
 * @static
 * @memberOf _
 * @category Objects
 * @param {*} value The value to clone.
 * @param {boolean} [isDeep=false] Specify a deep clone.
 * @param {Function} [callback] The function to customize cloning values.
 * @param {*} [thisArg] The `this` binding of `callback`.
 * @returns {*} Returns the cloned value.
 * @example
 *
 * var characters = [
 *   { 'name': 'barney', 'age': 36 },
 *   { 'name': 'fred',   'age': 40 }
 * ];
 *
 * var shallow = _.clone(characters***REMOVED***;
 * shallow[0] === characters[0];
 * // => true
 *
 * var deep = _.clone(characters, true***REMOVED***;
 * deep[0] === characters[0];
 * // => false
 *
 * _.mixin({
 *   'clone': _.partialRight(_.clone, function(value***REMOVED*** {
 *     return _.isElement(value***REMOVED*** ? value.cloneNode(false***REMOVED*** : undefined;
 * ***REMOVED******REMOVED***
 * }***REMOVED***;
 *
 * var clone = _.clone(document.body***REMOVED***;
 * clone.childNodes.length;
 * // => 0
 */
function clone(value, isDeep, callback, thisArg***REMOVED*** {
  // allows working with "Collections" methods without using their `index`
  // and `collection` arguments for `isDeep` and `callback`
  if (typeof isDeep != 'boolean' && isDeep != null***REMOVED*** {
    thisArg = callback;
    callback = isDeep;
    isDeep = false;
***REMOVED***
  return baseClone(value, isDeep, typeof callback == 'function' && baseCreateCallback(callback, thisArg, 1***REMOVED******REMOVED***;
}

module.exports = clone;
