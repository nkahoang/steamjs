/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize exports="node" -o ./compat/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var assign = require('./assign'***REMOVED***,
    baseCreate = require('../internals/baseCreate'***REMOVED***;

/**
 * Creates an object that inherits from the given `prototype` object. If a
 * `properties` object is provided its own enumerable properties are assigned
 * to the created object.
 *
 * @static
 * @memberOf _
 * @category Objects
 * @param {Object} prototype The object to inherit from.
 * @param {Object} [properties] The properties to assign to the object.
 * @returns {Object} Returns the new object.
 * @example
 *
 * function Shape(***REMOVED*** {
 *   this.x = 0;
 *   this.y = 0;
 * }
 *
 * function Circle(***REMOVED*** {
 *   Shape.call(this***REMOVED***;
 * }
 *
 * Circle.prototype = _.create(Shape.prototype, { 'constructor': Circle }***REMOVED***;
 *
 * var circle = new Circle;
 * circle instanceof Circle;
 * // => true
 *
 * circle instanceof Shape;
 * // => true
 */
function create(prototype, properties***REMOVED*** {
  var result = baseCreate(prototype***REMOVED***;
  return properties ? assign(result, properties***REMOVED*** : result;
}

module.exports = create;
