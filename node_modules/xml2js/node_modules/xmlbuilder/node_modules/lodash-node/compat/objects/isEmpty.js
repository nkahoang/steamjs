/**
 * Lo-Dash 2.4.1 (Custom Build***REMOVED*** <http://lodash.com/>
 * Build: `lodash modularize exports="node" -o ./compat/`
 * Copyright 2012-2013 The Dojo Foundation <http://dojofoundation.org/>
 * Based on Underscore.js 1.5.2 <http://underscorejs.org/LICENSE>
 * Copyright 2009-2013 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 * Available under MIT license <http://lodash.com/license>
 */
var forOwn = require('./forOwn'***REMOVED***,
    isArguments = require('./isArguments'***REMOVED***,
    isFunction = require('./isFunction'***REMOVED***,
    support = require('../support'***REMOVED***;

/** `Object#toString` result shortcuts */
var argsClass = '[object Arguments]',
    arrayClass = '[object Array]',
    objectClass = '[object Object]',
    stringClass = '[object String]';

/** Used for native method references */
var objectProto = Object.prototype;

/** Used to resolve the internal [[Class]] of values */
var toString = objectProto.toString;

/**
 * Checks if `value` is empty. Arrays, strings, or `arguments` objects with a
 * length of `0` and objects with no own enumerable properties are considered
 * "empty".
 *
 * @static
 * @memberOf _
 * @category Objects
 * @param {Array|Object|string} value The value to inspect.
 * @returns {boolean} Returns `true` if the `value` is empty, else `false`.
 * @example
 *
 * _.isEmpty([1, 2, 3]***REMOVED***;
 * // => false
 *
 * _.isEmpty({}***REMOVED***;
 * // => true
 *
 * _.isEmpty(''***REMOVED***;
 * // => true
 */
function isEmpty(value***REMOVED*** {
  var result = true;
  if (!value***REMOVED*** {
    return result;
***REMOVED***
  var className = toString.call(value***REMOVED***,
      length = value.length;

  if ((className == arrayClass || className == stringClass ||
      (support.argsClass ? className == argsClass : isArguments(value***REMOVED******REMOVED******REMOVED*** ||
      (className == objectClass && typeof length == 'number' && isFunction(value.splice***REMOVED******REMOVED******REMOVED*** {
    return !length;
***REMOVED***
  forOwn(value, function(***REMOVED*** {
    return (result = false***REMOVED***;
***REMOVED******REMOVED***;
  return result;
}

module.exports = isEmpty;
