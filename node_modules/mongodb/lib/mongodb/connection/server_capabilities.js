var ServerCapabilities = function(isMasterResult***REMOVED*** {  
  // Capabilities
  var aggregationCursor = false;
  var writeCommands = false;
  var textSearch = false;
  var authCommands = false;
  var maxNumberOfDocsInBatch = isMasterResult.maxWriteBatchSize || 1000;

  if(isMasterResult.minWireVersion >= 0***REMOVED*** {
  	textSearch = true;
***REMOVED***

  if(isMasterResult.maxWireVersion >= 1***REMOVED*** {
  	aggregationCursor = true;
    authCommands = true;
***REMOVED***

  if(isMasterResult.maxWireVersion >= 2***REMOVED*** {
    writeCommands = true;
***REMOVED***

  // If no min or max wire version set to 0
  if(isMasterResult.minWireVersion == null***REMOVED*** {
    isMasterResult.minWireVersion = 0;
***REMOVED***

  if(isMasterResult.maxWireVersion == null***REMOVED*** {
    isMasterResult.maxWireVersion = 0;
***REMOVED***

  // Map up read only parameters
  setup_get_property(this, "hasAggregationCursor", aggregationCursor***REMOVED***;
  setup_get_property(this, "hasWriteCommands", writeCommands***REMOVED***;
  setup_get_property(this, "hasTextSearch", textSearch***REMOVED***;
  setup_get_property(this, "hasAuthCommands", authCommands***REMOVED***;
  setup_get_property(this, "minWireVersion", isMasterResult.minWireVersion***REMOVED***;
  setup_get_property(this, "maxWireVersion", isMasterResult.maxWireVersion***REMOVED***;
  setup_get_property(this, "maxNumberOfDocsInBatch", maxNumberOfDocsInBatch***REMOVED***;
}

var setup_get_property = function(object, name, value***REMOVED*** {
  Object.defineProperty(object, name, {
      enumerable: true
    , get: function (***REMOVED*** { return value; }
***REMOVED******REMOVED***;  
}

exports.ServerCapabilities = ServerCapabilities;