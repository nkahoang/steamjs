var Db = require('./db'***REMOVED***.Db
  , Server = require('./connection/server'***REMOVED***.Server
  , Mongos = require('./connection/mongos'***REMOVED***.Mongos
  , ReplSet = require('./connection/repl_set/repl_set'***REMOVED***.ReplSet
  , ReadPreference = require('./connection/read_preference'***REMOVED***.ReadPreference
  , inherits = require('util'***REMOVED***.inherits
  , EventEmitter = require('events'***REMOVED***.EventEmitter
  , parse = require('./connection/url_parser'***REMOVED***.parse;

/**
 * Create a new MongoClient instance.
 *
 * Options
 *  - **w**, {Number/String, > -1 || 'majority' || tag name} the write concern for the operation where < 1 is no acknowlegement of write and w >= 1, w = 'majority' or tag acknowledges the write
 *  - **wtimeout**, {Number, 0} set the timeout for waiting for write concern to finish (combines with w option***REMOVED***
 *  - **fsync**, (Boolean, default:false***REMOVED*** write waits for fsync before returning, from MongoDB 2.6 on, fsync cannot be combined with journal
 *  - **j**, (Boolean, default:false***REMOVED*** write waits for journal sync before returning
 *  - **readPreference** {String}, the prefered read preference (ReadPreference.PRIMARY, ReadPreference.PRIMARY_PREFERRED, ReadPreference.SECONDARY, ReadPreference.SECONDARY_PREFERRED, ReadPreference.NEAREST***REMOVED***.
 *  - **native_parser** {Boolean, default:false}, use c++ bson parser.
 *  - **forceServerObjectId** {Boolean, default:false}, force server to create _id fields instead of client.
 *  - **pkFactory** {Object}, object overriding the basic ObjectID primary key generation.
 *  - **serializeFunctions** {Boolean, default:false}, serialize functions.
 *  - **raw** {Boolean, default:false}, peform operations using raw bson buffers.
 *  - **recordQueryStats** {Boolean, default:false}, record query statistics during execution.
 *  - **retryMiliSeconds** {Number, default:5000}, number of miliseconds between retries.
 *  - **numberOfRetries** {Number, default:5}, number of retries off connection.
 *  - **bufferMaxEntries** {Boolean, default: -1}, sets a cap on how many operations the driver will buffer up before giving up on getting a working connection, default is -1 which is unlimited
 *
 * @class Represents a MongoClient
 * @param {Object} serverConfig server config object.
 * @param {Object} [options] additional options for the collection.
 */
function MongoClient(serverConfig, options***REMOVED*** {
  if(serverConfig != null***REMOVED*** {
    options = options == null ? {} : options;
    // If no write concern is set set the default to w:1
    if(options != null && !options.journal && !options.w && !options.fsync***REMOVED*** {
      options.w = 1;
  ***REMOVED***
    
    // The internal db instance we are wrapping
    this._db = new Db('test', serverConfig, options***REMOVED***;    
***REMOVED***
}

/**
 * @ignore
 */
inherits(MongoClient, EventEmitter***REMOVED***;

/**
 * Connect to MongoDB using a url as documented at
 *
 *  docs.mongodb.org/manual/reference/connection-string/
 *
 * Options
 *  - **uri_decode_auth** {Boolean, default:false} uri decode the user name and password for authentication
 *  - **db** {Object, default: null} a hash off options to set on the db object, see **Db constructor**
 *  - **server** {Object, default: null} a hash off options to set on the server objects, see **Server** constructor**
 *  - **replSet** {Object, default: null} a hash off options to set on the replSet object, see **ReplSet** constructor**
 *  - **mongos** {Object, default: null} a hash off options to set on the mongos object, see **Mongos** constructor**
 *
 * @param {String} url connection url for MongoDB.
 * @param {Object} [options] optional options for insert command
 * @param {Function} callback this will be called after executing this method. The first parameter will contain the Error object if an error occured, or null otherwise. While the second parameter will contain the initialized db object or null if an error occured.
 * @return {null}
 * @api public
 */
MongoClient.prototype.connect = function(url, options, callback***REMOVED*** {
  var self = this;

  if(typeof options == 'function'***REMOVED*** {
    callback = options;
    options = {};
***REMOVED***

  MongoClient.connect(url, options, function(err, db***REMOVED*** {
    if(err***REMOVED*** return callback(err, db***REMOVED***;
    // Store internal db instance reference
    self._db = db;
    // Emit open and perform callback
    self.emit("open", err, db***REMOVED***;
    callback(err, db***REMOVED***;
***REMOVED******REMOVED***;
}

/**
 * Initialize the database connection.
 *
 * @param {Function} callback this will be called after executing this method. The first parameter will contain the Error object if an error occured, or null otherwise. While the second parameter will contain the connected mongoclient or null if an error occured.
 * @return {null}
 * @api public
 */
MongoClient.prototype.open = function(callback***REMOVED*** {
  // Self reference
  var self = this;
  // Open the db
  this._db.open(function(err, db***REMOVED*** {
    if(err***REMOVED*** return callback(err, null***REMOVED***;
    // Emit open event
    self.emit("open", err, db***REMOVED***;
    // Callback
    callback(null, self***REMOVED***;
***REMOVED******REMOVED***
}

/**
 * Close the current db connection, including all the child db instances. Emits close event if no callback is provided.
 *
 * @param {Function} callback this will be called after executing this method. The first parameter will contain the Error object if an error occured, or null otherwise. While the second parameter will contain the results from the close method or null if an error occured.
 * @return {null}
 * @api public
 */
MongoClient.prototype.close = function(callback***REMOVED*** {
  this._db.close(callback***REMOVED***;
}

/**
 * Create a new Db instance sharing the current socket connections.
 *
 * @param {String} dbName the name of the database we want to use.
 * @return {Db} a db instance using the new database.
 * @api public
 */
MongoClient.prototype.db = function(dbName***REMOVED*** {
  return this._db.db(dbName***REMOVED***;
}

/**
 * Connect to MongoDB using a url as documented at
 *
 *  docs.mongodb.org/manual/reference/connection-string/
 *
 * Options
 *  - **uri_decode_auth** {Boolean, default:false} uri decode the user name and password for authentication
 *  - **db** {Object, default: null} a hash off options to set on the db object, see **Db constructor**
 *  - **server** {Object, default: null} a hash off options to set on the server objects, see **Server** constructor**
 *  - **replSet** {Object, default: null} a hash off options to set on the replSet object, see **ReplSet** constructor**
 *  - **mongos** {Object, default: null} a hash off options to set on the mongos object, see **Mongos** constructor**
 *
 * @param {String} url connection url for MongoDB.
 * @param {Object} [options] optional options for insert command
 * @param {Function} callback this will be called after executing this method. The first parameter will contain the Error object if an error occured, or null otherwise. While the second parameter will contain the initialized db object or null if an error occured.
 * @return {null}
 * @api public
 */
MongoClient.connect = function(url, options, callback***REMOVED*** {
  var args = Array.prototype.slice.call(arguments, 1***REMOVED***;
  callback = typeof args[args.length - 1] == 'function' ? args.pop(***REMOVED*** : null;
  options = args.length ? args.shift(***REMOVED*** : null;
  options = options || {};

  // Set default empty server options  
  var serverOptions = options.server || {};
  var mongosOptions = options.mongos || {};
  var replSetServersOptions = options.replSet || options.replSetServers || {};
  var dbOptions = options.db || {};

  // If callback is null throw an exception
  if(callback == null***REMOVED*** 
    throw new Error("no callback function provided"***REMOVED***;

  // Parse the string
  var object = parse(url, options***REMOVED***;

  // Merge in any options for db in options object
  if(dbOptions***REMOVED*** {
    for(var name in dbOptions***REMOVED*** object.db_options[name] = dbOptions[name];
***REMOVED***

  // Added the url to the options
  object.db_options.url = url;

  // Merge in any options for server in options object
  if(serverOptions***REMOVED*** {
    for(var name in serverOptions***REMOVED*** object.server_options[name] = serverOptions[name];
***REMOVED***

  // Merge in any replicaset server options
  if(replSetServersOptions***REMOVED*** {
    for(var name in replSetServersOptions***REMOVED*** object.rs_options[name] = replSetServersOptions[name];    
***REMOVED***

  // Merge in any replicaset server options
  if(mongosOptions***REMOVED*** {
    for(var name in mongosOptions***REMOVED*** object.mongos_options[name] = mongosOptions[name];    
***REMOVED***

  // We need to ensure that the list of servers are only either direct members or mongos
  // they cannot be a mix of monogs and mongod's
  var totalNumberOfServers = object.servers.length;
  var totalNumberOfMongosServers = 0;
  var totalNumberOfMongodServers = 0;
  var serverConfig = null;
  var errorServers = {};

  // Failure modes
  if(object.servers.length == 0***REMOVED*** throw new Error("connection string must contain at least one seed host"***REMOVED***;

  // If we have no db setting for the native parser try to set the c++ one first
  object.db_options.native_parser = _setNativeParser(object.db_options***REMOVED***;
  // If no auto_reconnect is set, set it to true as default for single servers
  if(typeof object.server_options.auto_reconnect != 'boolean'***REMOVED*** {
    object.server_options.auto_reconnect = true;
***REMOVED***

  // If we have more than a server, it could be replicaset or mongos list
  // need to verify that it's one or the other and fail if it's a mix
  // Connect to all servers and run ismaster
  for(var i = 0; i < object.servers.length; i++***REMOVED*** {
    // Set up socket options
    var _server_options = {
        poolSize:1
      , socketOptions: {
          connectTimeoutMS:30000 
        , socketTimeoutMS: 30000
    ***REMOVED***
      , auto_reconnect:false};

    // Ensure we have ssl setup for the servers
    if(object.rs_options.ssl***REMOVED*** {
      _server_options.ssl = object.rs_options.ssl;
      _server_options.sslValidate = object.rs_options.sslValidate;
      _server_options.sslCA = object.rs_options.sslCA;
      _server_options.sslCert = object.rs_options.sslCert;
      _server_options.sslKey = object.rs_options.sslKey;
      _server_options.sslPass = object.rs_options.sslPass;
  ***REMOVED*** else if(object.server_options.ssl***REMOVED*** {
      _server_options.ssl = object.server_options.ssl;
      _server_options.sslValidate = object.server_options.sslValidate;
      _server_options.sslCA = object.server_options.sslCA;
      _server_options.sslCert = object.server_options.sslCert;
      _server_options.sslKey = object.server_options.sslKey;
      _server_options.sslPass = object.server_options.sslPass;
  ***REMOVED***

    // Set up the Server object
    var _server = object.servers[i].domain_socket 
        ? new Server(object.servers[i].domain_socket, _server_options***REMOVED***
        : new Server(object.servers[i].host, object.servers[i].port, _server_options***REMOVED***;

    var connectFunction = function(__server***REMOVED*** { 
      // Attempt connect
      new Db(object.dbName, __server, {w:1, native_parser:false}***REMOVED***.open(function(err, db***REMOVED*** {
        // Update number of servers
        totalNumberOfServers = totalNumberOfServers - 1;          
        // If no error do the correct checks
        if(!err***REMOVED*** {
          // Close the connection
          db.close(true***REMOVED***;
          var isMasterDoc = db.serverConfig.isMasterDoc;
          // Check what type of server we have
          if(isMasterDoc.setName***REMOVED*** totalNumberOfMongodServers++;
          if(isMasterDoc.msg && isMasterDoc.msg == "isdbgrid"***REMOVED*** totalNumberOfMongosServers++;
      ***REMOVED*** else {
          errorServers[__server.host + ":" + __server.port] = __server;
      ***REMOVED***

        if(totalNumberOfServers == 0***REMOVED*** {
          // If we have a mix of mongod and mongos, throw an error
          if(totalNumberOfMongosServers > 0 && totalNumberOfMongodServers > 0***REMOVED*** {
            return process.nextTick(function(***REMOVED*** {
              try {
                callback(new Error("cannot combine a list of replicaset seeds and mongos seeds"***REMOVED******REMOVED***;
            ***REMOVED*** catch (err***REMOVED*** {
                if(db***REMOVED*** db.close(***REMOVED***;
                throw err
            ***REMOVED***              
          ***REMOVED******REMOVED***
        ***REMOVED***
          
          if(totalNumberOfMongodServers == 0 && object.servers.length == 1***REMOVED*** {
            var obj = object.servers[0];
            serverConfig = obj.domain_socket ? 
                new Server(obj.domain_socket, object.server_options***REMOVED***
              : new Server(obj.host, obj.port, object.server_options***REMOVED***;            
        ***REMOVED*** else if(totalNumberOfMongodServers > 0 || totalNumberOfMongosServers > 0***REMOVED*** {
            var finalServers = object.servers
              .filter(function(serverObj***REMOVED*** {
                return errorServers[serverObj.host + ":" + serverObj.port] == null;
            ***REMOVED******REMOVED***
              .map(function(serverObj***REMOVED*** {
                  return new Server(serverObj.host, serverObj.port, object.server_options***REMOVED***;
            ***REMOVED******REMOVED***;
            // Clean out any error servers
            errorServers = {};
            // Set up the final configuration
            if(totalNumberOfMongodServers > 0***REMOVED*** {
              serverConfig = new ReplSet(finalServers, object.rs_options***REMOVED***;                
          ***REMOVED*** else {
              serverConfig = new Mongos(finalServers, object.mongos_options***REMOVED***;                         
          ***REMOVED***
        ***REMOVED***

          if(serverConfig == null***REMOVED*** {
            return process.nextTick(function(***REMOVED*** {
              try {
                callback(new Error("Could not locate any valid servers in initial seed list"***REMOVED******REMOVED***;
            ***REMOVED*** catch (err***REMOVED*** {
                if(db***REMOVED*** db.close(***REMOVED***;
                throw err
            ***REMOVED***
          ***REMOVED******REMOVED***;
        ***REMOVED***
          // Ensure no firing off open event before we are ready
          serverConfig.emitOpen = false;
          // Set up all options etc and connect to the database
          _finishConnecting(serverConfig, object, options, callback***REMOVED***
      ***REMOVED***
    ***REMOVED******REMOVED***;        
  ***REMOVED***

    // Wrap the context of the call
    connectFunction(_server***REMOVED***;    
***REMOVED***    
}

var _setNativeParser = function(db_options***REMOVED*** {
  if(typeof db_options.native_parser == 'boolean'***REMOVED*** return db_options.native_parser;

  try {
    require('bson'***REMOVED***.BSONNative.BSON;
    return true;
***REMOVED*** catch(err***REMOVED*** {
    return false;
***REMOVED***
}

var _finishConnecting = function(serverConfig, object, options, callback***REMOVED*** {
  // Safe settings
  var safe = {};
  // Build the safe parameter if needed
  if(object.db_options.journal***REMOVED*** safe.j = object.db_options.journal;
  if(object.db_options.w***REMOVED*** safe.w = object.db_options.w;
  if(object.db_options.fsync***REMOVED*** safe.fsync = object.db_options.fsync;
  if(object.db_options.wtimeoutMS***REMOVED*** safe.wtimeout = object.db_options.wtimeoutMS;

  // If we have a read Preference set
  if(object.db_options.read_preference***REMOVED*** {
    var readPreference = new ReadPreference(object.db_options.read_preference***REMOVED***;
    // If we have the tags set up
    if(object.db_options.read_preference_tags***REMOVED***
      readPreference = new ReadPreference(object.db_options.read_preference, object.db_options.read_preference_tags***REMOVED***;
    // Add the read preference
    object.db_options.readPreference = readPreference;
***REMOVED***

  // No safe mode if no keys
  if(Object.keys(safe***REMOVED***.length == 0***REMOVED*** safe = false;

  // Add the safe object
  object.db_options.safe = safe;

  // Get the socketTimeoutMS
  var socketTimeoutMS = object.server_options.socketOptions.socketTimeoutMS || 0;

  // If we have a replset, override with replicaset socket timeout option if available
  if(serverConfig instanceof ReplSet***REMOVED*** {
    socketTimeoutMS = object.rs_options.socketOptions.socketTimeoutMS || socketTimeoutMS;
***REMOVED***

  // Set socketTimeout to the same as the connectTimeoutMS or 30 sec
  serverConfig.connectTimeoutMS = serverConfig.connectTimeoutMS || 30000;
  serverConfig.socketTimeoutMS = serverConfig.connectTimeoutMS;

  // Set up the db options
  var db = new Db(object.dbName, serverConfig, object.db_options***REMOVED***;
  // Open the db
  db.open(function(err, db***REMOVED***{
    if(err***REMOVED*** {
      return process.nextTick(function(***REMOVED*** {
        try {
          callback(err, null***REMOVED***;
      ***REMOVED*** catch (err***REMOVED*** {
          if(db***REMOVED*** db.close(***REMOVED***;
          throw err
      ***REMOVED***
    ***REMOVED******REMOVED***;
  ***REMOVED***

    // Reset the socket timeout
    serverConfig.socketTimeoutMS = socketTimeoutMS || 0;

    // Set the provided write concern or fall back to w:1 as default
    if(db.options !== null && !db.options.safe && !db.options.journal 
      && !db.options.w && !db.options.fsync && typeof db.options.w != 'number'
      && (db.options.safe == false && object.db_options.url.indexOf("safe="***REMOVED*** == -1***REMOVED******REMOVED*** {
        db.options.w = 1;
  ***REMOVED***

    if(err == null && object.auth***REMOVED***{
      // What db to authenticate against
      var authentication_db = db;
      if(object.db_options && object.db_options.authSource***REMOVED*** {
        authentication_db = db.db(object.db_options.authSource***REMOVED***;
    ***REMOVED***

      // Build options object
      var options = {};
      if(object.db_options.authMechanism***REMOVED*** options.authMechanism = object.db_options.authMechanism;
      if(object.db_options.gssapiServiceName***REMOVED*** options.gssapiServiceName = object.db_options.gssapiServiceName;

      // Authenticate
      authentication_db.authenticate(object.auth.user, object.auth.password, options, function(err, success***REMOVED***{
        if(success***REMOVED***{
          process.nextTick(function(***REMOVED*** {
            try {
              callback(null, db***REMOVED***;            
          ***REMOVED*** catch (err***REMOVED*** {
              if(db***REMOVED*** db.close(***REMOVED***;
              throw err
          ***REMOVED***
        ***REMOVED******REMOVED***;
      ***REMOVED*** else {
          if(db***REMOVED*** db.close(***REMOVED***;
          process.nextTick(function(***REMOVED*** {
            try {
              callback(err ? err : new Error('Could not authenticate user ' + object.auth[0]***REMOVED***, null***REMOVED***;
          ***REMOVED*** catch (err***REMOVED*** {
              if(db***REMOVED*** db.close(***REMOVED***;
              throw err
          ***REMOVED***
        ***REMOVED******REMOVED***;
      ***REMOVED***
    ***REMOVED******REMOVED***;
  ***REMOVED*** else {
      process.nextTick(function(***REMOVED*** {
        try {
          callback(err, db***REMOVED***;            
      ***REMOVED*** catch (err***REMOVED*** {
          if(db***REMOVED*** db.close(***REMOVED***;
          throw err
      ***REMOVED***
    ***REMOVED******REMOVED***
  ***REMOVED***
***REMOVED******REMOVED***;
}

exports.MongoClient = MongoClient;