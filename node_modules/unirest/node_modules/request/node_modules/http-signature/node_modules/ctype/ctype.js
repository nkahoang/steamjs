/*
 * rm - Feb 2011
 * ctype.js
 *
 * This module provides a simple abstraction towards reading and writing
 * different types of binary data. It is designed to use ctio.js and provide a
 * richer and more expressive API on top of it.
 *
 * By default we support the following as built in basic types:
 *	int8_t
 *	int16_t
 *	int32_t
 *	uint8_t
 *	uint16_t
 *	uint32_t
 *	uint64_t
 *	float
 *	double
 *	char
 *	char[]
 *
 * Each type is returned as a Number, with the exception of char and char[]
 * which are returned as Node Buffers. A char is considered a uint8_t.
 *
 * Requests to read and write data are specified as an array of JSON objects.
 * This is also the same way that one declares structs. Even if just a single
 * value is requested, it must be done as a struct. The array order determines
 * the order that we try and read values. Each entry has the following format
 * with values marked with a * being optional.
 *
 * { key: { type: /type/, value*: /value/, offset*: /offset/ }
 *
 * If offset is defined, we lseek(offset, SEEK_SET***REMOVED*** before reading the next
 * value. Value is defined when we're writing out data, otherwise it's ignored.
 *
 */

var mod_ctf = require('./ctf.js'***REMOVED***;
var mod_ctio = require('./ctio.js'***REMOVED***;
var mod_assert = require('assert'***REMOVED***;

/*
 * This is the set of basic types that we support.
 *
 *	read		The function to call to read in a value from a buffer
 *
 *	write		The function to call to write a value to a buffer
 *
 */
var deftypes = {
    'uint8_t':  { read: ctReadUint8, write: ctWriteUint8 },
    'uint16_t': { read: ctReadUint16, write: ctWriteUint16 },
    'uint32_t': { read: ctReadUint32, write: ctWriteUint32 },
    'uint64_t': { read: ctReadUint64, write: ctWriteUint64 },
    'int8_t': { read: ctReadSint8, write: ctWriteSint8 },
    'int16_t': { read: ctReadSint16, write: ctWriteSint16 },
    'int32_t': { read: ctReadSint32, write: ctWriteSint32 },
    'int64_t': { read: ctReadSint64, write: ctWriteSint64 },
    'float': { read: ctReadFloat, write: ctWriteFloat },
    'double': { read: ctReadDouble, write: ctWriteDouble },
    'char': { read: ctReadChar, write: ctWriteChar },
    'char[]': { read: ctReadCharArray, write: ctWriteCharArray }
};

/*
 * The following are wrappers around the CType IO low level API. They encode
 * knowledge about the size and return something in the expected format.
 */
function ctReadUint8(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.ruint8(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 1 }***REMOVED***;
}

function ctReadUint16(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.ruint16(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 2 }***REMOVED***;
}

function ctReadUint32(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.ruint32(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 4 }***REMOVED***;
}

function ctReadUint64(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.ruint64(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 8 }***REMOVED***;
}

function ctReadSint8(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.rsint8(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 1 }***REMOVED***;
}

function ctReadSint16(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.rsint16(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 2 }***REMOVED***;
}

function ctReadSint32(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.rsint32(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 4 }***REMOVED***;
}

function ctReadSint64(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.rsint64(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 8 }***REMOVED***;
}

function ctReadFloat(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.rfloat(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 4 }***REMOVED***;
}

function ctReadDouble(endian, buffer, offset***REMOVED***
{
	var val = mod_ctio.rdouble(buffer, endian, offset***REMOVED***;
	return ({ value: val, size: 8 }***REMOVED***;
}

/*
 * Reads a single character into a node buffer
 */
function ctReadChar(endian, buffer, offset***REMOVED***
{
	var res = new Buffer(1***REMOVED***;
	res[0] = mod_ctio.ruint8(buffer, endian, offset***REMOVED***;
	return ({ value: res, size: 1 }***REMOVED***;
}

function ctReadCharArray(length, endian, buffer, offset***REMOVED***
{
	var ii;
	var res = new Buffer(length***REMOVED***;

	for (ii = 0; ii < length; ii++***REMOVED***
		res[ii] = mod_ctio.ruint8(buffer, endian, offset + ii***REMOVED***;

	return ({ value: res, size: length }***REMOVED***;
}

function ctWriteUint8(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wuint8(value, endian, buffer, offset***REMOVED***;
	return (1***REMOVED***;
}

function ctWriteUint16(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wuint16(value, endian, buffer, offset***REMOVED***;
	return (2***REMOVED***;
}

function ctWriteUint32(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wuint32(value, endian, buffer, offset***REMOVED***;
	return (4***REMOVED***;
}

function ctWriteUint64(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wuint64(value, endian, buffer, offset***REMOVED***;
	return (8***REMOVED***;
}

function ctWriteSint8(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wsint8(value, endian, buffer, offset***REMOVED***;
	return (1***REMOVED***;
}

function ctWriteSint16(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wsint16(value, endian, buffer, offset***REMOVED***;
	return (2***REMOVED***;
}

function ctWriteSint32(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wsint32(value, endian, buffer, offset***REMOVED***;
	return (4***REMOVED***;
}

function ctWriteSint64(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wsint64(value, endian, buffer, offset***REMOVED***;
	return (8***REMOVED***;
}

function ctWriteFloat(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wfloat(value, endian, buffer, offset***REMOVED***;
	return (4***REMOVED***;
}

function ctWriteDouble(value, endian, buffer, offset***REMOVED***
{
	mod_ctio.wdouble(value, endian, buffer, offset***REMOVED***;
	return (8***REMOVED***;
}

/*
 * Writes a single character into a node buffer
 */
function ctWriteChar(value, endian, buffer, offset***REMOVED***
{
	if (!(value instanceof Buffer***REMOVED******REMOVED***
		throw (new Error('Input must be a buffer'***REMOVED******REMOVED***;

	mod_ctio.ruint8(value[0], endian, buffer, offset***REMOVED***;
	return (1***REMOVED***;
}

/*
 * We're going to write 0s into the buffer if the string is shorter than the
 * length of the array.
 */
function ctWriteCharArray(value, length, endian, buffer, offset***REMOVED***
{
	var ii;

	if (!(value instanceof Buffer***REMOVED******REMOVED***
		throw (new Error('Input must be a buffer'***REMOVED******REMOVED***;

	if (value.length > length***REMOVED***
		throw (new Error('value length greater than array length'***REMOVED******REMOVED***;

	for (ii = 0; ii < value.length && ii < length; ii++***REMOVED***
		mod_ctio.wuint8(value[ii], endian, buffer, offset + ii***REMOVED***;

	for (; ii < length; ii++***REMOVED***
		mod_ctio.wuint8(0, endian, offset + ii***REMOVED***;


	return (length***REMOVED***;
}

/*
 * Each parser has their own set of types. We want to make sure that they each
 * get their own copy as they may need to modify it.
 */
function ctGetBasicTypes(***REMOVED***
{
	var ret = {};
	var key;
	for (key in deftypes***REMOVED***
		ret[key] = deftypes[key];

	return (ret***REMOVED***;
}

/*
 * Given a string in the form of type[length] we want to split this into an
 * object that extracts that information. We want to note that we could possibly
 * have nested arrays so this should only check the furthest one. It may also be
 * the case that we have no [] pieces, in which case we just return the current
 * type.
 */
function ctParseType(str***REMOVED***
{
	var begInd, endInd;
	var type, len;
	if (typeof (str***REMOVED*** != 'string'***REMOVED***
		throw (new Error('type must be a Javascript string'***REMOVED******REMOVED***;

	endInd = str.lastIndexOf(']'***REMOVED***;
	if (endInd == -1***REMOVED*** {
		if (str.lastIndexOf('['***REMOVED*** != -1***REMOVED***
			throw (new Error('found invalid type with \'[\' but ' +
			    'no corresponding \']\''***REMOVED******REMOVED***;

		return ({ type: str }***REMOVED***;
	}

	begInd = str.lastIndexOf('['***REMOVED***;
	if (begInd == -1***REMOVED***
		throw (new Error('found invalid type with \']\' but ' +
		    'no corresponding \'[\''***REMOVED******REMOVED***;

	if (begInd >= endInd***REMOVED***
		throw (new Error('malformed type, \']\' appears before \'[\''***REMOVED******REMOVED***;

	type = str.substring(0, begInd***REMOVED***;
	len = str.substring(begInd + 1, endInd***REMOVED***;

	return ({ type: type, len: len }***REMOVED***;
}

/*
 * Given a request validate that all of the fields for it are valid and make
 * sense. This includes verifying the following notions:
 *  - Each type requested is present in types
 *  - Only allow a name for a field to be specified once
 *  - If an array is specified, validate that the requested field exists and
 *    comes before it.
 *  - If fields is defined, check that each entry has the occurrence of field
 */
function ctCheckReq(def, types, fields***REMOVED***
{
	var ii, jj;
	var req, keys, key;
	var found = {};

	if (!(def instanceof Array***REMOVED******REMOVED***
		throw (new Error('definition is not an array'***REMOVED******REMOVED***;

	if (def.length === 0***REMOVED***
		throw (new Error('definition must have at least one element'***REMOVED******REMOVED***;

	for (ii = 0; ii < def.length; ii++***REMOVED*** {
		req = def[ii];
		if (!(req instanceof Object***REMOVED******REMOVED***
			throw (new Error('definition must be an array of' +
			    'objects'***REMOVED******REMOVED***;

		keys = Object.keys(req***REMOVED***;
		if (keys.length != 1***REMOVED***
			throw (new Error('definition entry must only have ' +
			    'one key'***REMOVED******REMOVED***;

		if (keys[0] in found***REMOVED***
			throw (new Error('Specified name already ' +
			    'specified: ' + keys[0]***REMOVED******REMOVED***;

		if (!('type' in req[keys[0]]***REMOVED******REMOVED***
			throw (new Error('missing required type definition'***REMOVED******REMOVED***;

		key = ctParseType(req[keys[0]]['type']***REMOVED***;

		/*
		 * We may have nested arrays, we need to check the validity of
		 * the types until the len field is undefined in key. However,
		 * each time len is defined we need to verify it is either an
		 * integer or corresponds to an already seen key.
		 */
		while (key['len'] !== undefined***REMOVED*** {
			if (isNaN(parseInt(key['len'], 10***REMOVED******REMOVED******REMOVED*** {
				if (!(key['len'] in found***REMOVED******REMOVED***
					throw (new Error('Given an array ' +
					    'length without a matching type'***REMOVED******REMOVED***;

			}

			key = ctParseType(key['type']***REMOVED***;
		}

		/* Now we can validate if the type is valid */
		if (!(key['type'] in types***REMOVED******REMOVED***
			throw (new Error('type not found or typdefed: ' +
			    key['type']***REMOVED******REMOVED***;

		/* Check for any required fields */
		if (fields !== undefined***REMOVED*** {
			for (jj = 0; jj < fields.length; jj++***REMOVED*** {
				if (!(fields[jj] in req[keys[0]]***REMOVED******REMOVED***
					throw (new Error('Missing required ' +
					    'field: ' + fields[jj]***REMOVED******REMOVED***;
			}
		}

		found[keys[0]] = true;
	}
}


/*
 * Create a new instance of the parser. Each parser has its own store of
 * typedefs and endianness. Conf is an object with the following required
 * values:
 *
 *	endian		Either 'big' or 'little' do determine the endianness we
 *			want to read from or write to.
 *
 * And the following optional values:
 *
 * 	char-type	Valid options here are uint8 and int8. If uint8 is
 * 			specified this changes the default behavior of a single
 * 			char from being a buffer of a single character to being
 * 			a uint8_t. If int8, it becomes an int8_t instead.
 */
function CTypeParser(conf***REMOVED***
{
	if (!conf***REMOVED*** throw (new Error('missing required argument'***REMOVED******REMOVED***;

	if (!('endian' in conf***REMOVED******REMOVED***
		throw (new Error('missing required endian value'***REMOVED******REMOVED***;

	if (conf['endian'] != 'big' && conf['endian'] != 'little'***REMOVED***
		throw (new Error('Invalid endian type'***REMOVED******REMOVED***;

	if ('char-type' in conf && (conf['char-type'] != 'uint8' &&
	    conf['char-type'] != 'int8'***REMOVED******REMOVED***
		throw (new Error('invalid option for char-type: ' +
		    conf['char-type']***REMOVED******REMOVED***;

	this.endian = conf['endian'];
	this.types = ctGetBasicTypes(***REMOVED***;

	/*
	 * There may be a more graceful way to do this, but this will have to
	 * serve.
	 */
	if ('char-type' in conf && conf['char-type'] == 'uint8'***REMOVED***
		this.types['char'] = this.types['uint8_t'];

	if ('char-type' in conf && conf['char-type'] == 'int8'***REMOVED***
		this.types['char'] = this.types['int8_t'];
}

/*
 * Sets the current endian value for the Parser. If the value is not valid,
 * throws an Error.
 *
 *	endian		Either 'big' or 'little' do determine the endianness we
 *			want to read from or write to.
 *
 */
CTypeParser.prototype.setEndian = function (endian***REMOVED***
{
	if (endian != 'big' && endian != 'little'***REMOVED***
		throw (new Error('invalid endian type, must be big or ' +
		    'little'***REMOVED******REMOVED***;

	this.endian = endian;
};

/*
 * Returns the current value of the endian value for the parser.
 */
CTypeParser.prototype.getEndian = function (***REMOVED***
{
	return (this.endian***REMOVED***;
};

/*
 * A user has requested to add a type, let us honor their request. Yet, if their
 * request doth spurn us, send them unto the Hells which Dante describes.
 *
 * 	name		The string for the type definition we're adding
 *
 *	value		Either a string that is a type/array name or an object
 *			that describes a struct.
 */
CTypeParser.prototype.typedef = function (name, value***REMOVED***
{
	var type;

	if (name === undefined***REMOVED***
		throw (new (Error('missing required typedef argument: name'***REMOVED******REMOVED******REMOVED***;

	if (value === undefined***REMOVED***
		throw (new (Error('missing required typedef argument: value'***REMOVED******REMOVED******REMOVED***;

	if (typeof (name***REMOVED*** != 'string'***REMOVED***
		throw (new (Error('the name of a type must be a string'***REMOVED******REMOVED******REMOVED***;

	type = ctParseType(name***REMOVED***;

	if (type['len'] !== undefined***REMOVED***
		throw (new Error('Cannot have an array in the typedef name'***REMOVED******REMOVED***;

	if (name in this.types***REMOVED***
		throw (new Error('typedef name already present: ' + name***REMOVED******REMOVED***;

	if (typeof (value***REMOVED*** != 'string' && !(value instanceof Array***REMOVED******REMOVED***
		throw (new Error('typedef value must either be a string or ' +
		    'struct'***REMOVED******REMOVED***;

	if (typeof (value***REMOVED*** == 'string'***REMOVED*** {
		type = ctParseType(value***REMOVED***;
		if (type['len'] !== undefined***REMOVED*** {
			if (isNaN(parseInt(type['len'], 10***REMOVED******REMOVED******REMOVED***
				throw (new (Error('typedef value must use ' +
				    'fixed size array when outside of a ' +
				    'struct'***REMOVED******REMOVED******REMOVED***;
		}

		this.types[name] = value;
	} else {
		/* We have a struct, validate it */
		ctCheckReq(value, this.types***REMOVED***;
		this.types[name] = value;
	}
};

/*
 * Include all of the typedefs, but none of the built in types. This should be
 * treated as read-only.
 */
CTypeParser.prototype.lstypes = function (***REMOVED***
{
	var key;
	var ret = {};

	for (key in this.types***REMOVED*** {
		if (key in deftypes***REMOVED***
			continue;
		ret[key] = this.types[key];
	}

	return (ret***REMOVED***;
};

/*
 * Given a type string that may have array types that aren't numbers, try and
 * fill them in from the values object. The object should be of the format where
 * indexing into it should return a number for that type.
 *
 *	str		The type string
 *
 *	values		An object that can be used to fulfill type information
 */
function ctResolveArray(str, values***REMOVED***
{
	var ret = '';
	var type = ctParseType(str***REMOVED***;

	while (type['len'] !== undefined***REMOVED*** {
		if (isNaN(parseInt(type['len'], 10***REMOVED******REMOVED******REMOVED*** {
			if (typeof (values[type['len']]***REMOVED*** != 'number'***REMOVED***
				throw (new Error('cannot sawp in non-number ' +
				    'for array value'***REMOVED******REMOVED***;
			ret = '[' + values[type['len']] + ']' + ret;
		} else {
			ret = '[' + type['len'] + ']' + ret;
		}
		type = ctParseType(type['type']***REMOVED***;
	}

	ret = type['type'] + ret;

	return (ret***REMOVED***;
}

/*
 * [private] Either the typedef resolves to another type string or to a struct.
 * If it resolves to a struct, we just pass it off to read struct. If not, we
 * can just pass it off to read entry.
 */
CTypeParser.prototype.resolveTypedef = function (type, dispatch, buffer,
    offset, value***REMOVED***
{
	var pt;

	mod_assert.ok(type in this.types***REMOVED***;
	if (typeof (this.types[type]***REMOVED*** == 'string'***REMOVED*** {
		pt = ctParseType(this.types[type]***REMOVED***;
		if (dispatch == 'read'***REMOVED***
			return (this.readEntry(pt, buffer, offset***REMOVED******REMOVED***;
		else if (dispatch == 'write'***REMOVED***
			return (this.writeEntry(value, pt, buffer, offset***REMOVED******REMOVED***;
		else
			throw (new Error('invalid dispatch type to ' +
			    'resolveTypedef'***REMOVED******REMOVED***;
	} else {
		if (dispatch == 'read'***REMOVED***
			return (this.readStruct(this.types[type], buffer,
			    offset***REMOVED******REMOVED***;
		else if (dispatch == 'write'***REMOVED***
			return (this.writeStruct(value, this.types[type],
			    buffer, offset***REMOVED******REMOVED***;
		else
			throw (new Error('invalid dispatch type to ' +
			    'resolveTypedef'***REMOVED******REMOVED***;
	}

};

/*
 * [private] Try and read in the specific entry.
 */
CTypeParser.prototype.readEntry = function (type, buffer, offset***REMOVED***
{
	var parse, len;

	/*
	 * Because we want to special case char[]s this is unfortunately
	 * a bit uglier than it really should be. We want to special
	 * case char[]s so that we return a node buffer, thus they are a
	 * first class type where as all other arrays just call into a
	 * generic array routine which calls their data-specific routine
	 * the specified number of times.
	 *
	 * The valid dispatch options we have are:
	 *  - Array and char => char[] handler
	 *  - Generic array handler
	 *  - Generic typedef handler
	 *  - Basic type handler
	 */
	if (type['len'] !== undefined***REMOVED*** {
		len = parseInt(type['len'], 10***REMOVED***;
		if (isNaN(len***REMOVED******REMOVED***
			throw (new Error('somehow got a non-numeric length'***REMOVED******REMOVED***;

		if (type['type'] == 'char'***REMOVED***
			parse = this.types['char[]']['read'](len,
			    this.endian, buffer, offset***REMOVED***;
		else
			parse = this.readArray(type['type'],
			    len, buffer, offset***REMOVED***;
	} else {
		if (type['type'] in deftypes***REMOVED***
			parse = this.types[type['type']]['read'](this.endian,
			    buffer, offset***REMOVED***;
		else
			parse = this.resolveTypedef(type['type'], 'read',
			    buffer, offset***REMOVED***;
	}

	return (parse***REMOVED***;
};

/*
 * [private] Read an array of data
 */
CTypeParser.prototype.readArray = function (type, length, buffer, offset***REMOVED***
{
	var ii, ent, pt;
	var baseOffset = offset;
	var ret = new Array(length***REMOVED***;
	pt = ctParseType(type***REMOVED***;

	for (ii = 0; ii < length; ii++***REMOVED*** {
		ent = this.readEntry(pt, buffer, offset***REMOVED***;
		offset += ent['size'];
		ret[ii] = ent['value'];
	}

	return ({ value: ret, size: offset - baseOffset }***REMOVED***;
};

/*
 * [private] Read a single struct in.
 */
CTypeParser.prototype.readStruct = function (def, buffer, offset***REMOVED***
{
	var parse, ii, type, entry, key;
	var baseOffset = offset;
	var ret = {};

	/* Walk it and handle doing what's necessary */
	for (ii = 0; ii < def.length; ii++***REMOVED*** {
		key = Object.keys(def[ii]***REMOVED***[0];
		entry = def[ii][key];

		/* Resolve all array values */
		type = ctParseType(ctResolveArray(entry['type'], ret***REMOVED******REMOVED***;

		if ('offset' in entry***REMOVED***
			offset = baseOffset + entry['offset'];

		parse = this.readEntry(type, buffer, offset***REMOVED***;

		offset += parse['size'];
		ret[key] = parse['value'];
	}

	return ({ value: ret, size: (offset-baseOffset***REMOVED***}***REMOVED***;
};

/*
 * This is what we were born to do. We read the data from a buffer and return it
 * in an object whose keys match the values from the object.
 *
 *	def		The array definition of the data to read in
 *
 *	buffer		The buffer to read data from
 *
 *	offset		The offset to start writing to
 *
 * Returns an object where each key corresponds to an entry in def and the value
 * is the read value.
 */
CTypeParser.prototype.readData = function (def, buffer, offset***REMOVED***
{
	/* Sanity check for arguments */
	if (def === undefined***REMOVED***
		throw (new Error('missing definition for what we should be' +
		    'parsing'***REMOVED******REMOVED***;

	if (buffer === undefined***REMOVED***
		throw (new Error('missing buffer for what we should be ' +
		    'parsing'***REMOVED******REMOVED***;

	if (offset === undefined***REMOVED***
		throw (new Error('missing offset for what we should be ' +
		    'parsing'***REMOVED******REMOVED***;

	/* Sanity check the object definition */
	ctCheckReq(def, this.types***REMOVED***;

	return (this.readStruct(def, buffer, offset***REMOVED***['value']***REMOVED***;
};

/*
 * [private] Write out an array of data
 */
CTypeParser.prototype.writeArray = function (value, type, length, buffer,
    offset***REMOVED***
{
	var ii, pt;
	var baseOffset = offset;
	if (!(value instanceof Array***REMOVED******REMOVED***
		throw (new Error('asked to write an array, but value is not ' +
		    'an array'***REMOVED******REMOVED***;

	if (value.length != length***REMOVED***
		throw (new Error('asked to write array of length ' + length +
		    ' but that does not match value length: ' + value.length***REMOVED******REMOVED***;

	pt = ctParseType(type***REMOVED***;
	for (ii = 0; ii < length; ii++***REMOVED***
		offset += this.writeEntry(value[ii], pt, buffer, offset***REMOVED***;

	return (offset - baseOffset***REMOVED***;
};

/*
 * [private] Write the specific entry
 */
CTypeParser.prototype.writeEntry = function (value, type, buffer, offset***REMOVED***
{
	var len, ret;

	if (type['len'] !== undefined***REMOVED*** {
		len = parseInt(type['len'], 10***REMOVED***;
		if (isNaN(len***REMOVED******REMOVED***
			throw (new Error('somehow got a non-numeric length'***REMOVED******REMOVED***;

		if (type['type'] == 'char'***REMOVED***
			ret = this.types['char[]']['write'](value, len,
			    this.endian, buffer, offset***REMOVED***;
		else
			ret = this.writeArray(value, type['type'],
			    len, buffer, offset***REMOVED***;
	} else {
		if (type['type'] in deftypes***REMOVED***
			ret = this.types[type['type']]['write'](value,
			    this.endian, buffer, offset***REMOVED***;
		else
			ret = this.resolveTypedef(type['type'], 'write',
			    buffer, offset, value***REMOVED***;
	}

	return (ret***REMOVED***;
};

/*
 * [private] Write a single struct out.
 */
CTypeParser.prototype.writeStruct = function (value, def, buffer, offset***REMOVED***
{
	var ii, entry, type, key;
	var baseOffset = offset;
	var vals = {};

	for (ii = 0; ii < def.length; ii++***REMOVED*** {
		key = Object.keys(def[ii]***REMOVED***[0];
		entry = def[ii][key];

		type = ctParseType(ctResolveArray(entry['type'], vals***REMOVED******REMOVED***;

		if ('offset' in entry***REMOVED***
			offset = baseOffset + entry['offset'];

		offset += this.writeEntry(value[ii], type, buffer, offset***REMOVED***;
		/* Now that we've written it out, we can use it for arrays */
		vals[key] = value[ii];
	}

	return (offset***REMOVED***;
};

/*
 * Unfortunately, we're stuck with the sins of an initial poor design. Because
 * of that, we are going to have to support the old way of writing data via
 * writeData. There we insert the values that you want to write into the
 * definition. A little baroque. Internally, we use the new model. So we need to
 * just get those values out of there. But to maintain the principle of least
 * surprise, we're not going to modify the input data.
 */
function getValues(def***REMOVED***
{
	var ii, out, key;
	out = [];
	for (ii = 0; ii < def.length; ii++***REMOVED*** {
		key = Object.keys(def[ii]***REMOVED***[0];
		mod_assert.ok('value' in def[ii][key]***REMOVED***;
		out.push(def[ii][key]['value']***REMOVED***;
	}

	return (out***REMOVED***;
}

/*
 * This is the second half of what we were born to do, write out the data
 * itself. Historically this function required you to put your values in the
 * definition section. This was not the smartest thing to do and a bit of an
 * oversight to be honest. As such, this function now takes a values argument.
 * If values is non-null and non-undefined, it will be used to determine the
 * values. This means that the old method is still supported, but is no longer
 * acceptable.
 *
 *	def		The array definition of the data to write out with
 *			values
 *
 *	buffer		The buffer to write to
 *
 *	offset		The offset in the buffer to write to
 *
 *	values		An array of values to write.
 */
CTypeParser.prototype.writeData = function (def, buffer, offset, values***REMOVED***
{
	var hv;

	if (def === undefined***REMOVED***
		throw (new Error('missing definition for what we should be' +
		    'parsing'***REMOVED******REMOVED***;

	if (buffer === undefined***REMOVED***
		throw (new Error('missing buffer for what we should be ' +
		    'parsing'***REMOVED******REMOVED***;

	if (offset === undefined***REMOVED***
		throw (new Error('missing offset for what we should be ' +
		    'parsing'***REMOVED******REMOVED***;

	hv = (values != null && values != undefined***REMOVED***;
	if (hv***REMOVED*** {
		if (!Array.isArray(values***REMOVED******REMOVED***
			throw (new Error('missing values for writing'***REMOVED******REMOVED***;
		ctCheckReq(def, this.types***REMOVED***;
	} else {
		ctCheckReq(def, this.types, [ 'value' ]***REMOVED***;
	}

	this.writeStruct(hv ? values : getValues(def***REMOVED***, def, buffer, offset***REMOVED***;
};

/*
 * Functions to go to and from 64 bit numbers in a way that is compatible with
 * Javascript limitations. There are two sets. One where the user is okay with
 * an approximation and one where they are definitely not okay with an
 * approximation.
 */

/*
 * Attempts to convert an array of two integers returned from rsint64 / ruint64
 * into an absolute 64 bit number. If however the value would exceed 2^52 this
 * will instead throw an error. The mantissa in a double is a 52 bit number and
 * rather than potentially give you a value that is an approximation this will
 * error. If you would rather an approximation, please see toApprox64.
 *
 *	val		An array of two 32-bit integers
 */
function toAbs64(val***REMOVED***
{
	if (val === undefined***REMOVED***
		throw (new Error('missing required arg: value'***REMOVED******REMOVED***;

	if (!Array.isArray(val***REMOVED******REMOVED***
		throw (new Error('value must be an array'***REMOVED******REMOVED***;

	if (val.length != 2***REMOVED***
		throw (new Error('value must be an array of length 2'***REMOVED******REMOVED***;

	/* We have 20 bits worth of precision in this range */
	if (val[0] >= 0x100000***REMOVED***
		throw (new Error('value would become approximated'***REMOVED******REMOVED***;

	return (val[0] * Math.pow(2, 32***REMOVED*** + val[1]***REMOVED***;
}

/*
 * Will return the 64 bit value as returned in an array from rsint64 / ruint64
 * to a value as close as it can. Note that Javascript stores all numbers as a
 * double and the mantissa only has 52 bits. Thus this version may approximate
 * the value.
 *
 *	val		An array of two 32-bit integers
 */
function toApprox64(val***REMOVED***
{
	if (val === undefined***REMOVED***
		throw (new Error('missing required arg: value'***REMOVED******REMOVED***;

	if (!Array.isArray(val***REMOVED******REMOVED***
		throw (new Error('value must be an array'***REMOVED******REMOVED***;

	if (val.length != 2***REMOVED***
		throw (new Error('value must be an array of length 2'***REMOVED******REMOVED***;

	return (Math.pow(2, 32***REMOVED*** * val[0] + val[1]***REMOVED***;
}

function parseCTF(json, conf***REMOVED***
{
	var ctype = new CTypeParser(conf***REMOVED***;
	mod_ctf.ctfParseJson(json, ctype***REMOVED***;

	return (ctype***REMOVED***;
}

/*
 * Export the few things we actually want to. Currently this is just the CType
 * Parser and ctio.
 */
exports.Parser = CTypeParser;
exports.toAbs64 = toAbs64;
exports.toApprox64 = toApprox64;

exports.parseCTF = parseCTF;

exports.ruint8 = mod_ctio.ruint8;
exports.ruint16 = mod_ctio.ruint16;
exports.ruint32 = mod_ctio.ruint32;
exports.ruint64 = mod_ctio.ruint64;
exports.wuint8 = mod_ctio.wuint8;
exports.wuint16 = mod_ctio.wuint16;
exports.wuint32 = mod_ctio.wuint32;
exports.wuint64 = mod_ctio.wuint64;

exports.rsint8 = mod_ctio.rsint8;
exports.rsint16 = mod_ctio.rsint16;
exports.rsint32 = mod_ctio.rsint32;
exports.rsint64 = mod_ctio.rsint64;
exports.wsint8 = mod_ctio.wsint8;
exports.wsint16 = mod_ctio.wsint16;
exports.wsint32 = mod_ctio.wsint32;
exports.wsint64 = mod_ctio.wsint64;

exports.rfloat = mod_ctio.rfloat;
exports.rdouble = mod_ctio.rdouble;
exports.wfloat = mod_ctio.wfloat;
exports.wdouble = mod_ctio.wdouble;
