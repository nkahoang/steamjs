var common = require('../common'***REMOVED***;
var assert = common.assert;
var fake = common.fake.create(***REMOVED***;
var DelayedStream = common.DelayedStream;
var Stream = require('stream'***REMOVED***.Stream;

(function testHandleSourceErrors(***REMOVED*** {
  var source = new Stream(***REMOVED***;
  var delayedStream = DelayedStream.create(source, {pauseStream: false}***REMOVED***;

  // We deal with this by attaching a no-op listener to 'error' on the source
  // when creating a new DelayedStream. This way error events on the source
  // won't throw.
  source.emit('error', new Error('something went wrong'***REMOVED******REMOVED***;
}***REMOVED***(***REMOVED***;
