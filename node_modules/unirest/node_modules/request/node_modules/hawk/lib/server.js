// Load modules

var Boom = require('boom'***REMOVED***;
var Hoek = require('hoek'***REMOVED***;
var Cryptiles = require('cryptiles'***REMOVED***;
var Crypto = require('./crypto'***REMOVED***;
var Utils = require('./utils'***REMOVED***;


// Declare internals

var internals = {};


// Hawk authentication

/*
   req:                 node's HTTP request object or an object as follows:
  
                        var request = {
                            method: 'GET',
                            url: '/resource/4?a=1&b=2',
                            host: 'example.com',
                            port: 8080,
                            authorization: 'Hawk id="dh37fgj492je", ts="1353832234", nonce="j4h3g2", ext="some-app-ext-data", mac="6R4rV5iE+NPoym+WwjeHzjAGXUtLNIxmo1vpMofpLAE="'
                      ***REMOVED***;
  
   credentialsFunc:     required function to lookup the set of Hawk credentials based on the provided credentials id.
                        The credentials include the MAC key, MAC algorithm, and other attributes (such as username***REMOVED***
                        needed by the application. This function is the equivalent of verifying the username and
                        password in Basic authentication.
  
                        var credentialsFunc = function (id, callback***REMOVED*** {
    
                            // Lookup credentials in database
                            db.lookup(id, function (err, item***REMOVED*** {
    
                                if (err || !item***REMOVED*** {
                                    return callback(err***REMOVED***;
                              ***REMOVED***
    
                                var credentials = {
                                    // Required
                                    key: item.key,
                                    algorithm: item.algorithm,
                                    // Application specific
                                    user: item.user
                              ***REMOVED***;
    
                                return callback(null, credentials***REMOVED***;
                          ***REMOVED******REMOVED***;
                      ***REMOVED***;
  
   options: {

        hostHeaderName:        optional header field name, used to override the default 'Host' header when used
                               behind a cache of a proxy. Apache2 changes the value of the 'Host' header while preserving
                               the original (which is what the module must verify***REMOVED*** in the 'x-forwarded-host' header field.
                               Only used when passed a node Http.ServerRequest object.
  
        nonceFunc:             optional nonce validation function. The function signature is function(nonce, ts, callback***REMOVED***
                               where 'callback' must be called using the signature function(err***REMOVED***.
  
        timestampSkewSec:      optional number of seconds of permitted clock skew for incoming timestamps. Defaults to 60 seconds.
                               Provides a +/- skew which means actual allowed window is double the number of seconds.
  
        localtimeOffsetMsec:   optional local clock time offset express in a number of milliseconds (positive or negative***REMOVED***.
                               Defaults to 0.
  
        payload:               optional payload for validation. The client calculates the hash value and includes it via the 'hash'
                               header attribute. The server always ensures the value provided has been included in the request
                               MAC. When this option is provided, it validates the hash value itself. Validation is done by calculating
                               a hash value over the entire payload (assuming it has already be normalized to the same format and
                               encoding used by the client to calculate the hash on request***REMOVED***. If the payload is not available at the time
                               of authentication, the authenticatePayload(***REMOVED*** method can be used by passing it the credentials and
                               attributes.hash returned in the authenticate callback.

        host:                  optional host name override. Only used when passed a node request object.
        port:                  optional port override. Only used when passed a node request object.
  ***REMOVED***

    callback: function (err, credentials, artifacts***REMOVED*** { }
 */

exports.authenticate = function (req, credentialsFunc, options, callback***REMOVED*** {

    callback = Utils.nextTick(callback***REMOVED***;
    
    // Default options

    options.nonceFunc = options.nonceFunc || function (nonce, ts, nonceCallback***REMOVED*** { return nonceCallback(***REMOVED***; };   // No validation
    options.timestampSkewSec = options.timestampSkewSec || 60;                                                  // 60 seconds

    // Application time

    var now = Utils.now(***REMOVED*** + (options.localtimeOffsetMsec || 0***REMOVED***;                 // Measure now before any other processing

    // Convert node Http request object to a request configuration object

    var request = Utils.parseRequest(req, options***REMOVED***;
    if (request instanceof Error***REMOVED*** {
        return callback(Boom.badRequest(request.message***REMOVED******REMOVED***;
  ***REMOVED***

    // Parse HTTP Authorization header

    var attributes = Utils.parseAuthorizationHeader(request.authorization***REMOVED***;
    if (attributes instanceof Error***REMOVED*** {
        return callback(attributes***REMOVED***;
  ***REMOVED***

    // Construct artifacts container

    var artifacts = {
        method: request.method,
        host: request.host,
        port: request.port,
        resource: request.url,
        ts: attributes.ts,
        nonce: attributes.nonce,
        hash: attributes.hash,
        ext: attributes.ext,
        app: attributes.app,
        dlg: attributes.dlg,
        mac: attributes.mac,
        id: attributes.id
  ***REMOVED***;

    // Verify required header attributes

    if (!attributes.id ||
        !attributes.ts ||
        !attributes.nonce ||
        !attributes.mac***REMOVED*** {

        return callback(Boom.badRequest('Missing attributes'***REMOVED***, null, artifacts***REMOVED***;
  ***REMOVED***

    // Fetch Hawk credentials

    credentialsFunc(attributes.id, function (err, credentials***REMOVED*** {

        if (err***REMOVED*** {
            return callback(err, credentials || null, artifacts***REMOVED***;
      ***REMOVED***

        if (!credentials***REMOVED*** {
            return callback(Boom.unauthorized('Unknown credentials', 'Hawk'***REMOVED***, null, artifacts***REMOVED***;
      ***REMOVED***

        if (!credentials.key ||
            !credentials.algorithm***REMOVED*** {

            return callback(Boom.internal('Invalid credentials'***REMOVED***, credentials, artifacts***REMOVED***;
      ***REMOVED***

        if (Crypto.algorithms.indexOf(credentials.algorithm***REMOVED*** === -1***REMOVED*** {
            return callback(Boom.internal('Unknown algorithm'***REMOVED***, credentials, artifacts***REMOVED***;
      ***REMOVED***

        // Calculate MAC

        var mac = Crypto.calculateMac('header', credentials, artifacts***REMOVED***;
        if (!Cryptiles.fixedTimeComparison(mac, attributes.mac***REMOVED******REMOVED*** {
            return callback(Boom.unauthorized('Bad mac', 'Hawk'***REMOVED***, credentials, artifacts***REMOVED***;
      ***REMOVED***

        // Check payload hash

        if (options.payload !== null &&
            options.payload !== undefined***REMOVED*** {       // '' is valid

            if (!attributes.hash***REMOVED*** {
                return callback(Boom.unauthorized('Missing required payload hash', 'Hawk'***REMOVED***, credentials, artifacts***REMOVED***;
          ***REMOVED***

            var hash = Crypto.calculatePayloadHash(options.payload, credentials.algorithm, request.contentType***REMOVED***;
            if (!Cryptiles.fixedTimeComparison(hash, attributes.hash***REMOVED******REMOVED*** {
                return callback(Boom.unauthorized('Bad payload hash', 'Hawk'***REMOVED***, credentials, artifacts***REMOVED***;
          ***REMOVED***
      ***REMOVED***

        // Check nonce

        options.nonceFunc(attributes.nonce, attributes.ts, function (err***REMOVED*** {

            if (err***REMOVED*** {
                return callback(Boom.unauthorized('Invalid nonce', 'Hawk'***REMOVED***, credentials, artifacts***REMOVED***;
          ***REMOVED***

            // Check timestamp staleness

            if (Math.abs((attributes.ts * 1000***REMOVED*** - now***REMOVED*** > (options.timestampSkewSec * 1000***REMOVED******REMOVED*** {
                var fresh = Math.floor((Utils.now(***REMOVED*** + (options.localtimeOffsetMsec || 0***REMOVED******REMOVED*** / 1000***REMOVED***;            // Get fresh now
                var tsm = Crypto.calculateTsMac(fresh, credentials***REMOVED***;
                return callback(Boom.unauthorized('Stale timestamp', 'Hawk', { ts: fresh, tsm: tsm }***REMOVED***, credentials, artifacts***REMOVED***;
          ***REMOVED***

            // Successful authentication

            return callback(null, credentials, artifacts***REMOVED***;
      ***REMOVED******REMOVED***;
  ***REMOVED******REMOVED***;
};


// Authenticate payload hash - used when payload cannot be provided during authenticate(***REMOVED***

/*
    payload:        raw request payload
    credentials:    from authenticate callback
    artifacts:      from authenticate callback
    contentType:    req.headers['content-type']
*/

exports.authenticatePayload = function (payload, credentials, artifacts, contentType***REMOVED*** {

    var calculatedHash = Crypto.calculatePayloadHash(payload, credentials.algorithm, contentType***REMOVED***;
    return Cryptiles.fixedTimeComparison(calculatedHash, artifacts.hash***REMOVED***;
};


// Generate a Server-Authorization header for a given response

/*
    credentials: {},                                        // Object received from authenticate(***REMOVED***
    artifacts: {}                                           // Object received from authenticate(***REMOVED***; 'mac', 'hash', and 'ext' - ignored
    options: {
        ext: 'application-specific',                        // Application specific data sent via the ext attribute
        payload: '{"some":"payload"}',                      // UTF-8 encoded string for body hash generation (ignored if hash provided***REMOVED***
        contentType: 'application/json',                    // Payload content-type (ignored if hash provided***REMOVED***
        hash: 'U4MKKSmiVxk37JCCrAVIjV='                     // Pre-calculated payload hash
  ***REMOVED***
*/

exports.header = function (credentials, artifacts, options***REMOVED*** {

    // Prepare inputs

    options = options || {};

    if (!artifacts ||
        typeof artifacts !== 'object' ||
        typeof options !== 'object'***REMOVED*** {

        return '';
  ***REMOVED***

    artifacts = Hoek.clone(artifacts***REMOVED***;
    delete artifacts.mac;
    artifacts.hash = options.hash;
    artifacts.ext = options.ext;

    // Validate credentials

    if (!credentials ||
        !credentials.key ||
        !credentials.algorithm***REMOVED*** {

        // Invalid credential object
        return '';
  ***REMOVED***

    if (Crypto.algorithms.indexOf(credentials.algorithm***REMOVED*** === -1***REMOVED*** {
        return '';
  ***REMOVED***

    // Calculate payload hash

    if (!artifacts.hash &&
        options.hasOwnProperty('payload'***REMOVED******REMOVED*** {

        artifacts.hash = Crypto.calculatePayloadHash(options.payload, credentials.algorithm, options.contentType***REMOVED***;
  ***REMOVED***

    var mac = Crypto.calculateMac('response', credentials, artifacts***REMOVED***;

    // Construct header

    var header = 'Hawk mac="' + mac + '"' +
                 (artifacts.hash ? ', hash="' + artifacts.hash + '"' : ''***REMOVED***;

    if (artifacts.ext !== null &&
        artifacts.ext !== undefined &&
        artifacts.ext !== ''***REMOVED*** {                       // Other falsey values allowed

        header += ', ext="' + Utils.escapeHeaderAttribute(artifacts.ext***REMOVED*** + '"';
  ***REMOVED***

    return header;
};


/*
 * Arguments and options are the same as authenticate(***REMOVED*** with the exception that the only supported options are:
 * 'hostHeaderName', 'localtimeOffsetMsec', 'host', 'port'
 */

exports.authenticateBewit = function (req, credentialsFunc, options, callback***REMOVED*** {

    callback = Utils.nextTick(callback***REMOVED***;

    // Application time

    var now = Utils.now(***REMOVED*** + (options.localtimeOffsetMsec || 0***REMOVED***;

    // Convert node Http request object to a request configuration object

    var request = Utils.parseRequest(req, options***REMOVED***;
    if (request instanceof Error***REMOVED*** {
        return callback(Boom.badRequest(request.message***REMOVED******REMOVED***;
  ***REMOVED***

    // Extract bewit

    //                                 1     2             3           4     
    var resource = request.url.match(/^(\/.****REMOVED***([\?&]***REMOVED***bewit\=([^&$]****REMOVED***(?:&(.+***REMOVED******REMOVED***?$/***REMOVED***;
    if (!resource***REMOVED*** {
        return callback(Boom.unauthorized(null, 'Hawk'***REMOVED******REMOVED***;
  ***REMOVED***

    // Bewit not empty

    if (!resource[3]***REMOVED*** {
        return callback(Boom.unauthorized('Empty bewit', 'Hawk'***REMOVED******REMOVED***;
  ***REMOVED***

    // Verify method is GET

    if (request.method !== 'GET' &&
        request.method !== 'HEAD'***REMOVED*** {

        return callback(Boom.unauthorized('Invalid method', 'Hawk'***REMOVED******REMOVED***;
  ***REMOVED***

    // No other authentication

    if (request.authorization***REMOVED*** {
        return callback(Boom.badRequest('Multiple authentications', 'Hawk'***REMOVED******REMOVED***;
  ***REMOVED***

    // Parse bewit

    var bewitString = Utils.base64urlDecode(resource[3]***REMOVED***;
    if (bewitString instanceof Error***REMOVED*** {
        return callback(Boom.badRequest('Invalid bewit encoding'***REMOVED******REMOVED***;
  ***REMOVED***

    // Bewit format: id\exp\mac\ext ('\' is used because it is a reserved header attribute character***REMOVED***

    var bewitParts = bewitString.split('\\'***REMOVED***;
    if (!bewitParts ||
        bewitParts.length !== 4***REMOVED*** {

        return callback(Boom.badRequest('Invalid bewit structure'***REMOVED******REMOVED***;
  ***REMOVED***

    var bewit = {
        id: bewitParts[0],
        exp: parseInt(bewitParts[1], 10***REMOVED***,
        mac: bewitParts[2],
        ext: bewitParts[3] || ''
  ***REMOVED***;

    if (!bewit.id ||
        !bewit.exp ||
        !bewit.mac***REMOVED*** {

        return callback(Boom.badRequest('Missing bewit attributes'***REMOVED******REMOVED***;
  ***REMOVED***

    // Construct URL without bewit

    var url = resource[1];
    if (resource[4]***REMOVED*** {
        url += resource[2] + resource[4];
  ***REMOVED***

    // Check expiration

    if (bewit.exp * 1000 <= now***REMOVED*** {
        return callback(Boom.unauthorized('Access expired', 'Hawk'***REMOVED***, null, bewit***REMOVED***;
  ***REMOVED***

    // Fetch Hawk credentials

    credentialsFunc(bewit.id, function (err, credentials***REMOVED*** {

        if (err***REMOVED*** {
            return callback(err, credentials || null, bewit.ext***REMOVED***;
      ***REMOVED***

        if (!credentials***REMOVED*** {
            return callback(Boom.unauthorized('Unknown credentials', 'Hawk'***REMOVED***, null, bewit***REMOVED***;
      ***REMOVED***

        if (!credentials.key ||
            !credentials.algorithm***REMOVED*** {

            return callback(Boom.internal('Invalid credentials'***REMOVED***, credentials, bewit***REMOVED***;
      ***REMOVED***

        if (Crypto.algorithms.indexOf(credentials.algorithm***REMOVED*** === -1***REMOVED*** {
            return callback(Boom.internal('Unknown algorithm'***REMOVED***, credentials, bewit***REMOVED***;
      ***REMOVED***

        // Calculate MAC

        var mac = Crypto.calculateMac('bewit', credentials, {
            ts: bewit.exp,
            nonce: '',
            method: 'GET',
            resource: url,
            host: request.host,
            port: request.port,
            ext: bewit.ext
      ***REMOVED******REMOVED***;

        if (!Cryptiles.fixedTimeComparison(mac, bewit.mac***REMOVED******REMOVED*** {
            return callback(Boom.unauthorized('Bad mac', 'Hawk'***REMOVED***, credentials, bewit***REMOVED***;
      ***REMOVED***

        // Successful authentication

        return callback(null, credentials, bewit***REMOVED***;
  ***REMOVED******REMOVED***;
};


/*
 *  options are the same as authenticate(***REMOVED*** with the exception that the only supported options are:
 * 'nonceFunc', 'timestampSkewSec', 'localtimeOffsetMsec'
 */

exports.authenticateMessage = function (host, port, message, authorization, credentialsFunc, options, callback***REMOVED*** {

    callback = Utils.nextTick(callback***REMOVED***;
    
    // Default options

    options.nonceFunc = options.nonceFunc || function (nonce, ts, nonceCallback***REMOVED*** { return nonceCallback(***REMOVED***; };   // No validation
    options.timestampSkewSec = options.timestampSkewSec || 60;                                                  // 60 seconds

    // Application time

    var now = Utils.now(***REMOVED*** + (options.localtimeOffsetMsec || 0***REMOVED***;                 // Measure now before any other processing

    // Validate authorization
    
    if (!authorization.id ||
        !authorization.ts ||
        !authorization.nonce ||
        !authorization.hash ||
        !authorization.mac***REMOVED*** {
        
            return callback(Boom.badRequest('Invalid authorization'***REMOVED******REMOVED***
  ***REMOVED***

    // Fetch Hawk credentials

    credentialsFunc(authorization.id, function (err, credentials***REMOVED*** {

        if (err***REMOVED*** {
            return callback(err, credentials || null***REMOVED***;
      ***REMOVED***

        if (!credentials***REMOVED*** {
            return callback(Boom.unauthorized('Unknown credentials', 'Hawk'***REMOVED******REMOVED***;
      ***REMOVED***

        if (!credentials.key ||
            !credentials.algorithm***REMOVED*** {

            return callback(Boom.internal('Invalid credentials'***REMOVED***, credentials***REMOVED***;
      ***REMOVED***

        if (Crypto.algorithms.indexOf(credentials.algorithm***REMOVED*** === -1***REMOVED*** {
            return callback(Boom.internal('Unknown algorithm'***REMOVED***, credentials***REMOVED***;
      ***REMOVED***

        // Construct artifacts container

        var artifacts = {
            ts: authorization.ts,
            nonce: authorization.nonce,
            host: host,
            port: port,
            hash: authorization.hash
      ***REMOVED***;

        // Calculate MAC

        var mac = Crypto.calculateMac('message', credentials, artifacts***REMOVED***;
        if (!Cryptiles.fixedTimeComparison(mac, authorization.mac***REMOVED******REMOVED*** {
            return callback(Boom.unauthorized('Bad mac', 'Hawk'***REMOVED***, credentials***REMOVED***;
      ***REMOVED***

        // Check payload hash

        var hash = Crypto.calculatePayloadHash(message, credentials.algorithm***REMOVED***;
        if (!Cryptiles.fixedTimeComparison(hash, authorization.hash***REMOVED******REMOVED*** {
            return callback(Boom.unauthorized('Bad message hash', 'Hawk'***REMOVED***, credentials***REMOVED***;
      ***REMOVED***

        // Check nonce

        options.nonceFunc(authorization.nonce, authorization.ts, function (err***REMOVED*** {

            if (err***REMOVED*** {
                return callback(Boom.unauthorized('Invalid nonce', 'Hawk'***REMOVED***, credentials***REMOVED***;
          ***REMOVED***

            // Check timestamp staleness

            if (Math.abs((authorization.ts * 1000***REMOVED*** - now***REMOVED*** > (options.timestampSkewSec * 1000***REMOVED******REMOVED*** {
                return callback(Boom.unauthorized('Stale timestamp'***REMOVED***, credentials***REMOVED***;
          ***REMOVED***

            // Successful authentication

            return callback(null, credentials***REMOVED***;
      ***REMOVED******REMOVED***;
  ***REMOVED******REMOVED***;
};
