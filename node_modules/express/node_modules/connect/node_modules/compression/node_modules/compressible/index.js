module.exports = compressible

compressible.specs =
compressible.specifications = require('./specifications.json'***REMOVED***

compressible.regex =
compressible.regexp = /json|text|javascript|dart|ecmascript|xml/

compressible.get = get

function compressible(type***REMOVED*** {
  if (!type || typeof type !== "string"***REMOVED*** return false
  var i = type.indexOf(';'***REMOVED***
    , spec = compressible.specs[~i ? type.slice(0, i***REMOVED*** : type]
  return spec ? spec.compressible : compressible.regex.test(type***REMOVED***
}

function get(type***REMOVED*** {
  if (!type || typeof type !== "string"***REMOVED*** return {
    compressible: false,
    sources: [],
    notes: "Invalid type."
***REMOVED***
  var spec = compressible.specs[type.split(';'***REMOVED***[0]]
  return spec ? spec : {
    compressible: compressible.regex.test(type***REMOVED***,
    sources: ["compressible.regex"],
    notes: "Automatically generated via regex."
***REMOVED***
}