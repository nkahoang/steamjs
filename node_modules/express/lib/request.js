
/**
 * Module dependencies.
 */

var ***REMOVED***
  , utils = require('./utils'***REMOVED***
  , connect = require('connect'***REMOVED***
  , fresh = require('fresh'***REMOVED***
  , parseRange = require('range-parser'***REMOVED***
  , parse = connect.utils.parseUrl
  , mime = connect.mime;

/**
 * Request prototype.
 */

var req = exports = module.exports = {
  __proto__: http.IncomingMessage.prototype
};

/**
 * Return request header.
 *
 * The `Referrer` header field is special-cased,
 * both `Referrer` and `Referer` are interchangeable.
 *
 * Examples:
 *
 *     req.get('Content-Type'***REMOVED***;
 *     // => "text/plain"
 *
 *     req.get('content-type'***REMOVED***;
 *     // => "text/plain"
 *
 *     req.get('Something'***REMOVED***;
 *     // => undefined
 *
 * Aliased as `req.header(***REMOVED***`.
 *
 * @param {String} name
 * @return {String}
 * @api public
 */

req.get =
req.header = function(name***REMOVED***{
  switch (name = name.toLowerCase(***REMOVED******REMOVED*** {
    case 'referer':
    case 'referrer':
      return this.headers.referrer
        || this.headers.referer;
    default:
      return this.headers[name];
***REMOVED***
};

/**
 * Check if the given `type(s***REMOVED***` is acceptable, returning
 * the best match when true, otherwise `undefined`, in which
 * case you should respond with 406 "Not Acceptable".
 *
 * The `type` value may be a single mime type string
 * such as "application/json", the extension name
 * such as "json", a comma-delimted list such as "json, html, text/plain",
 * an argument list such as `"json", "html", "text/plain"`,
 * or an array `["json", "html", "text/plain"]`. When a list
 * or array is given the _best_ match, if any is returned.
 *
 * Examples:
 *
 *     // Accept: text/html
 *     req.accepts('html'***REMOVED***;
 *     // => "html"
 *
 *     // Accept: text/*, application/json
 *     req.accepts('html'***REMOVED***;
 *     // => "html"
 *     req.accepts('text/html'***REMOVED***;
 *     // => "text/html"
 *     req.accepts('json, text'***REMOVED***;
 *     // => "json"
 *     req.accepts('application/json'***REMOVED***;
 *     // => "application/json"
 *
 *     // Accept: text/*, application/json
 *     req.accepts('image/png'***REMOVED***;
 *     req.accepts('png'***REMOVED***;
 *     // => undefined
 *
 *     // Accept: text/*;q=.5, application/json
 *     req.accepts(['html', 'json']***REMOVED***;
 *     req.accepts('html', 'json'***REMOVED***;
 *     req.accepts('html, json'***REMOVED***;
 *     // => "json"
 *
 * @param {String|Array} type(s***REMOVED***
 * @return {String}
 * @api public
 */

req.accepts = function(type***REMOVED***{
  var args = arguments.length > 1 ? [].slice.apply(arguments***REMOVED*** : type;
  return utils.accepts(args, this.get('Accept'***REMOVED******REMOVED***;
};

/**
 * Check if the given `encoding` is accepted.
 *
 * @param {String} encoding
 * @return {Boolean}
 * @api public
 */

req.acceptsEncoding = function(encoding***REMOVED***{
  return !! ~this.acceptedEncodings.indexOf(encoding***REMOVED***;
};

/**
 * Check if the given `charset` is acceptable,
 * otherwise you should respond with 406 "Not Acceptable".
 *
 * @param {String} charset
 * @return {Boolean}
 * @api public
 */

req.acceptsCharset = function(charset***REMOVED***{
  var accepted = this.acceptedCharsets;
  return accepted.length
    ? !! ~accepted.indexOf(charset***REMOVED***
    : true;
};

/**
 * Check if the given `lang` is acceptable,
 * otherwise you should respond with 406 "Not Acceptable".
 *
 * @param {String} lang
 * @return {Boolean}
 * @api public
 */

req.acceptsLanguage = function(lang***REMOVED***{
  var accepted = this.acceptedLanguages;
  return accepted.length
    ? !! ~accepted.indexOf(lang***REMOVED***
    : true;
};

/**
 * Parse Range header field,
 * capping to the given `size`.
 *
 * Unspecified ranges such as "0-" require
 * knowledge of your resource length. In
 * the case of a byte range this is of course
 * the total number of bytes. If the Range
 * header field is not given `null` is returned,
 * `-1` when unsatisfiable, `-2` when syntactically invalid.
 *
 * NOTE: remember that ranges are inclusive, so
 * for example "Range: users=0-3" should respond
 * with 4 users when available, not 3.
 *
 * @param {Number} size
 * @return {Array}
 * @api public
 */

req.range = function(size***REMOVED***{
  var range = this.get('Range'***REMOVED***;
  if (!range***REMOVED*** return;
  return parseRange(size, range***REMOVED***;
};

/**
 * Return an array of encodings.
 *
 * Examples:
 *
 *     ['gzip', 'deflate']
 *
 * @return {Array}
 * @api public
 */

req.__defineGetter__('acceptedEncodings', function(***REMOVED***{
  var accept = this.get('Accept-Encoding'***REMOVED***;
  return accept
    ? accept.trim(***REMOVED***.split(/ *, */***REMOVED***
    : [];
}***REMOVED***;

/**
 * Return an array of Accepted media types
 * ordered from highest quality to lowest.
 *
 * Examples:
 *
 *     [ { value: 'application/json',
 *         quality: 1,
 *         type: 'application',
 *         subtype: 'json' },
 *       { value: 'text/html',
 *         quality: 0.5,
 *         type: 'text',
 *         subtype: 'html' } ]
 *
 * @return {Array}
 * @api public
 */

req.__defineGetter__('accepted', function(***REMOVED***{
  var accept = this.get('Accept'***REMOVED***;
  return accept
    ? utils.parseAccept(accept***REMOVED***
    : [];
}***REMOVED***;

/**
 * Return an array of Accepted languages
 * ordered from highest quality to lowest.
 *
 * Examples:
 *
 *     Accept-Language: en;q=.5, en-us
 *     ['en-us', 'en']
 *
 * @return {Array}
 * @api public
 */

req.__defineGetter__('acceptedLanguages', function(***REMOVED***{
  var accept = this.get('Accept-Language'***REMOVED***;
  return accept
    ? utils
      .parseParams(accept***REMOVED***
      .map(function(obj***REMOVED***{
        return obj.value;
    ***REMOVED******REMOVED***
    : [];
}***REMOVED***;

/**
 * Return an array of Accepted charsets
 * ordered from highest quality to lowest.
 *
 * Examples:
 *
 *     Accept-Charset: iso-8859-5;q=.2, unicode-1-1;q=0.8
 *     ['unicode-1-1', 'iso-8859-5']
 *
 * @return {Array}
 * @api public
 */

req.__defineGetter__('acceptedCharsets', function(***REMOVED***{
  var accept = this.get('Accept-Charset'***REMOVED***;
  return accept
    ? utils
      .parseParams(accept***REMOVED***
      .map(function(obj***REMOVED***{
        return obj.value;
    ***REMOVED******REMOVED***
    : [];
}***REMOVED***;

/**
 * Return the value of param `name` when present or `defaultValue`.
 *
 *  - Checks route placeholders, ex: _/user/:id_
 *  - Checks body params, ex: id=12, {"id":12}
 *  - Checks query string params, ex: ?id=12
 *
 * To utilize request bodies, `req.body`
 * should be an object. This can be done by using
 * the `connect.bodyParser(***REMOVED***` middleware.
 *
 * @param {String} name
 * @param {Mixed} [defaultValue]
 * @return {String}
 * @api public
 */

req.param = function(name, defaultValue***REMOVED***{
  var params = this.params || {};
  var body = this.body || {};
  var query = this.query || {};
  if (null != params[name] && params.hasOwnProperty(name***REMOVED******REMOVED*** return params[name];
  if (null != body[name]***REMOVED*** return body[name];
  if (null != query[name]***REMOVED*** return query[name];
  return defaultValue;
};

/**
 * Check if the incoming request contains the "Content-Type"
 * header field, and it contains the give mime `type`.
 *
 * Examples:
 *
 *      // With Content-Type: text/html; charset=utf-8
 *      req.is('html'***REMOVED***;
 *      req.is('text/html'***REMOVED***;
 *      req.is('text/*'***REMOVED***;
 *      // => true
 *
 *      // When Content-Type is application/json
 *      req.is('json'***REMOVED***;
 *      req.is('application/json'***REMOVED***;
 *      req.is('application/*'***REMOVED***;
 *      // => true
 *
 *      req.is('html'***REMOVED***;
 *      // => false
 *
 * @param {String} type
 * @return {Boolean}
 * @api public
 */

req.is = function(type***REMOVED***{
  var ct = this.get('Content-Type'***REMOVED***;
  if (!ct***REMOVED*** return false;
  ct = ct.split(';'***REMOVED***[0];
  if (!~type.indexOf('/'***REMOVED******REMOVED*** type = mime.lookup(type***REMOVED***;
  if (~type.indexOf('*'***REMOVED******REMOVED*** {
    type = type.split('/'***REMOVED***;
    ct = ct.split('/'***REMOVED***;
    if ('*' == type[0] && type[1] == ct[1]***REMOVED*** return true;
    if ('*' == type[1] && type[0] == ct[0]***REMOVED*** return true;
    return false;
***REMOVED***
  return !! ~ct.indexOf(type***REMOVED***;
};

/**
 * Return the protocol string "http" or "https"
 * when requested with TLS. When the "trust proxy"
 * setting is enabled the "X-Forwarded-Proto" header
 * field will be trusted. If you're running behind
 * a reverse proxy that supplies https for you this
 * may be enabled.
 *
 * @return {String}
 * @api public
 */

req.__defineGetter__('protocol', function(***REMOVED***{
  var trustProxy = this.app.get('trust proxy'***REMOVED***;
  if (this.connection.encrypted***REMOVED*** return 'https';
  if (!trustProxy***REMOVED*** return 'http';
  var proto = this.get('X-Forwarded-Proto'***REMOVED*** || 'http';
  return proto.split(/\s*,\s*/***REMOVED***[0];
}***REMOVED***;

/**
 * Short-hand for:
 *
 *    req.protocol == 'https'
 *
 * @return {Boolean}
 * @api public
 */

req.__defineGetter__('secure', function(***REMOVED***{
  return 'https' == this.protocol;
}***REMOVED***;

/**
 * Return the remote address, or when
 * "trust proxy" is `true` return
 * the upstream addr.
 *
 * @return {String}
 * @api public
 */

req.__defineGetter__('ip', function(***REMOVED***{
  return this.ips[0] || this.connection.remoteAddress;
}***REMOVED***;

/**
 * When "trust proxy" is `true`, parse
 * the "X-Forwarded-For" ip address list.
 *
 * For example if the value were "client, proxy1, proxy2"
 * you would receive the array `["client", "proxy1", "proxy2"]`
 * where "proxy2" is the furthest down-stream.
 *
 * @return {Array}
 * @api public
 */

req.__defineGetter__('ips', function(***REMOVED***{
  var trustProxy = this.app.get('trust proxy'***REMOVED***;
  var val = this.get('X-Forwarded-For'***REMOVED***;
  return trustProxy && val
    ? val.split(/ *, */***REMOVED***
    : [];
}***REMOVED***;

/**
 * Return basic auth credentials.
 *
 * Examples:
 *
 *    // http://tobi:hello@example.com
 *    req.auth
 *    // => { username: 'tobi', password: 'hello' }
 *
 * @return {Object} or undefined
 * @api public
 */

req.__defineGetter__('auth', function(***REMOVED***{
  // missing
  var auth = this.get('Authorization'***REMOVED***;
  if (!auth***REMOVED*** return;

  // malformed
  var parts = auth.split(' '***REMOVED***;
  if ('basic' != parts[0].toLowerCase(***REMOVED******REMOVED*** return;
  if (!parts[1]***REMOVED*** return;
  auth = parts[1];

  // credentials
  auth = new Buffer(auth, 'base64'***REMOVED***.toString(***REMOVED***.match(/^([^:]****REMOVED***:(.****REMOVED***$/***REMOVED***;
  if (!auth***REMOVED*** return;
  return { username: auth[1], password: auth[2] };
}***REMOVED***;

/**
 * Return subdomains as an array.
 *
 * Subdomains are the dot-separated parts of the host before the main domain of
 * the app. By default, the domain of the app is assumed to be the last two
 * parts of the host. This can be changed by setting "subdomain offset".
 *
 * For example, if the domain is "tobi.ferrets.example.com":
 * If "subdomain offset" is not set, req.subdomains is `["ferrets", "tobi"]`.
 * If "subdomain offset" is 3, req.subdomains is `["tobi"]`.
 *
 * @return {Array}
 * @api public
 */

req.__defineGetter__('subdomains', function(***REMOVED***{
  var offset = this.app.get('subdomain offset'***REMOVED***;
  return (this.host || ''***REMOVED***
    .split('.'***REMOVED***
    .reverse(***REMOVED***
    .slice(offset***REMOVED***;
}***REMOVED***;

/**
 * Short-hand for `url.parse(req.url***REMOVED***.pathname`.
 *
 * @return {String}
 * @api public
 */

req.__defineGetter__('path', function(***REMOVED***{
  return parse(this***REMOVED***.pathname;
}***REMOVED***;

/**
 * Parse the "Host" header field hostname.
 *
 * @return {String}
 * @api public
 */

req.__defineGetter__('host', function(***REMOVED***{
  var trustProxy = this.app.get('trust proxy'***REMOVED***;
  var host = trustProxy && this.get('X-Forwarded-Host'***REMOVED***;
  host = host || this.get('Host'***REMOVED***;
  if (!host***REMOVED*** return;
  return host.split(':'***REMOVED***[0];
}***REMOVED***;

/**
 * Check if the request is fresh, aka
 * Last-Modified and/or the ETag
 * still match.
 *
 * @return {Boolean}
 * @api public
 */

req.__defineGetter__('fresh', function(***REMOVED***{
  var method = this.method;
  var s = this.res.statusCode;

  // GET or HEAD for weak freshness validation only
  if ('GET' != method && 'HEAD' != method***REMOVED*** return false;

  // 2xx or 304 as per rfc2616 14.26
  if ((s >= 200 && s < 300***REMOVED*** || 304 == s***REMOVED*** {
    return fresh(this.headers, this.res._headers***REMOVED***;
***REMOVED***

  return false;
}***REMOVED***;

/**
 * Check if the request is stale, aka
 * "Last-Modified" and / or the "ETag" for the
 * resource has changed.
 *
 * @return {Boolean}
 * @api public
 */

req.__defineGetter__('stale', function(***REMOVED***{
  return !this.fresh;
}***REMOVED***;

/**
 * Check if the request was an _XMLHttpRequest_.
 *
 * @return {Boolean}
 * @api public
 */

req.__defineGetter__('xhr', function(***REMOVED***{
  var val = this.get('X-Requested-With'***REMOVED*** || '';
  return 'xmlhttprequest' == val.toLowerCase(***REMOVED***;
}***REMOVED***;
