
/*!
 * Stylus - Visitor
 * Copyright(c***REMOVED*** 2010 LearnBoost <dev@learnboost.com>
 * MIT Licensed
 */

/**
 * Initialize a new `Visitor` with the given `root` Node.
 *
 * @param {Node} root
 * @api private
 */

var Visitor = module.exports = function Visitor(root***REMOVED*** {
  this.root = root;
};

/**
 * Visit the given `node`.
 *
 * @param {Node|Array} node
 * @api public
 */

Visitor.prototype.visit = function(node, fn***REMOVED***{
  var method = 'visit' + node.constructor.name;
  if (this[method]***REMOVED*** return this[method](node***REMOVED***;
  return node;
};

