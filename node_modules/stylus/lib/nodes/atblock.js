/*!
 * Stylus - @block
 * Copyright(c***REMOVED*** 2010 LearnBoost <dev@learnboost.com>
 * MIT Licensed
 */

/**
 * Module dependencies.
 */

var Node = require('./node'***REMOVED***;

/**
 * Initialize a new `@block` node.
 *
 * @api public
 */

var Atblock = module.exports = function Atblock(***REMOVED***{
  Node.call(this***REMOVED***;
};

/**
 * Return `block` nodes.
 */

Atblock.prototype.__defineGetter__('nodes', function(***REMOVED***{
  return this.block.nodes;
}***REMOVED***;

/**
 * Inherit from `Node.prototype`.
 */

Atblock.prototype.__proto__ = Node.prototype;

/**
 * Return a clone of this node.
 *
 * @return {Node}
 * @api public
 */

Atblock.prototype.clone = function(***REMOVED***{
  var clone = new Atblock;
  clone.block = this.block.clone(***REMOVED***;
  clone.lineno = this.lineno;
  clone.filename = this.filename;
  return clone;
};

/**
 * Return @block.
 *
 * @return {String}
 * @api public
 */

Atblock.prototype.toString = function(***REMOVED***{
  return '@block';
};
