
/*!
 * Stylus - QueryList
 * Copyright(c***REMOVED*** 2014 LearnBoost <dev@learnboost.com>
 * MIT Licensed
 */

/**
 * Module dependencies.
 */

var Node = require('./node'***REMOVED***;

/**
 * Initialize a new `QueryList`.
 *
 * @api public
 */

var QueryList = module.exports = function QueryList(***REMOVED***{
  Node.call(this***REMOVED***;
  this.nodes = [];
};

/**
 * Inherit from `Node.prototype`.
 */

QueryList.prototype.__proto__ = Node.prototype;

/**
 * Return a clone of this node.
 * 
 * @return {Node}
 * @api public
 */

QueryList.prototype.clone = function(***REMOVED***{
  var clone = new QueryList;
  clone.lineno = this.lineno;
  clone.filename = this.filename;
  for (var i = 0; i < this.nodes.length; ++i***REMOVED*** {
    clone.push(this.nodes[i].clone(***REMOVED******REMOVED***;
***REMOVED***
  return clone;
};

/**
 * Push the given `node`.
 *
 * @param {Node} node
 * @api public
 */

QueryList.prototype.push = function(node***REMOVED***{
  this.nodes.push(node***REMOVED***;
};

/**
 * Return "<a>, <b>, <c>"
 *
 * @return {String}
 * @api public
 */

QueryList.prototype.toString = function(***REMOVED***{
  return '(' + this.nodes.map(function(node***REMOVED***{
    return node.toString(***REMOVED***;
***REMOVED******REMOVED***.join(', '***REMOVED*** + '***REMOVED***';
};

