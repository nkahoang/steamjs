
/*!
 * Stylus - Selector
 * Copyright(c***REMOVED*** 2010 LearnBoost <dev@learnboost.com>
 * MIT Licensed
 */

/**
 * Module dependencies.
 */

var Block = require('./block'***REMOVED***
  , Node = require('./node'***REMOVED***;

/**
 * Initialize a new `Selector` with the given `segs`.
 *
 * @param {Array} segs
 * @api public
 */

var Selector = module.exports = function Selector(segs***REMOVED***{
  Node.call(this***REMOVED***;
  this.inherits = true;
  this.segments = segs;
};

/**
 * Inherit from `Node.prototype`.
 */

Selector.prototype.__proto__ = Node.prototype;

/**
 * Return the selector string.
 *
 * @return {String}
 * @api public
 */

Selector.prototype.toString = function(***REMOVED***{
  return this.segments.join(''***REMOVED***;
};

/**
 * Check if this is placeholder selector.
 *
 * @return {Boolean}
 * @api public
 */

Selector.prototype.__defineGetter__('isPlaceholder', function(***REMOVED***{
  return this.val && ~this.val.substr(0, 2***REMOVED***.indexOf('$'***REMOVED***;
}***REMOVED***;

/**
 * Return a clone of this node.
 * 
 * @return {Node}
 * @api public
 */

Selector.prototype.clone = function(***REMOVED***{
  var clone = new Selector;
  clone.lineno = this.lineno;
  clone.filename = this.filename;
  clone.segments = this.segments.map(function(node***REMOVED***{ return node.clone(***REMOVED***; }***REMOVED***;
  return clone;
};
