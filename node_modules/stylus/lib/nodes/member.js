
/*!
 * Stylus - Member
 * Copyright(c***REMOVED*** 2010 LearnBoost <dev@learnboost.com>
 * MIT Licensed
 */

/**
 * Module dependencies.
 */

var Node = require('./node'***REMOVED***;

/**
 * Initialize a new `Member` with `left` and `right`.
 *
 * @param {Node} left
 * @param {Node} right
 * @api public
 */

var Member = module.exports = function Member(left, right***REMOVED***{
  Node.call(this***REMOVED***;
  this.left = left;
  this.right = right;
};

/**
 * Inherit from `Node.prototype`.
 */

Member.prototype.__proto__ = Node.prototype;

/**
 * Return a clone of this node.
 *
 * @return {Node}
 * @api public
 */

Member.prototype.clone = function(***REMOVED***{
  var clone = new Member(this.left.clone(***REMOVED***, this.right.clone(***REMOVED******REMOVED***;
  clone.lineno = this.lineno;
  clone.filename = this.filename;
  if (this.val***REMOVED*** clone.val = this.val.clone(***REMOVED***;
  return clone;
};

/**
 * Return a string representation of this node.
 *
 * @return {String}
 * @api public
 */

Member.prototype.toString = function(***REMOVED***{
  return this.left.toString(***REMOVED***
    + '.' + this.right.toString(***REMOVED***;
};
