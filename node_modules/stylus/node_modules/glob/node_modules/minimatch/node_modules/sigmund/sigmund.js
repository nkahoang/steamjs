module.exports = sigmund
function sigmund (subject, maxSessions***REMOVED*** {
    maxSessions = maxSessions || 10;
    var notes = [];
    var analysis = '';
    var RE = RegExp;

    function psychoAnalyze (subject, session***REMOVED*** {
        if (session > maxSessions***REMOVED*** return;

        if (typeof subject === 'function' ||
            typeof subject === 'undefined'***REMOVED*** {
            return;
      ***REMOVED***

        if (typeof subject !== 'object' || !subject ||
            (subject instanceof RE***REMOVED******REMOVED*** {
            analysis += subject;
            return;
      ***REMOVED***

        if (notes.indexOf(subject***REMOVED*** !== -1 || session === maxSessions***REMOVED*** return;

        notes.push(subject***REMOVED***;
        analysis += '{';
        Object.keys(subject***REMOVED***.forEach(function (issue, _, __***REMOVED*** {
            // pseudo-private values.  skip those.
            if (issue.charAt(0***REMOVED*** === '_'***REMOVED*** return;
            var to = typeof subject[issue];
            if (to === 'function' || to === 'undefined'***REMOVED*** return;
            analysis += issue;
            psychoAnalyze(subject[issue], session + 1***REMOVED***;
      ***REMOVED******REMOVED***;
  ***REMOVED***
    psychoAnalyze(subject, 0***REMOVED***;
    return analysis;
}

// vim: set softtabstop=4 shiftwidth=4:
