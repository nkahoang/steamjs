var mkdirp = require('../'***REMOVED***;
var ***REMOVED***;
var fs = require('fs'***REMOVED***;
var test = require('tap'***REMOVED***.test;

test('return value', function (t***REMOVED*** {
    t.plan(2***REMOVED***;
    var x = Math.floor(Math.random(***REMOVED*** * Math.pow(16,4***REMOVED******REMOVED***.toString(16***REMOVED***;
    var y = Math.floor(Math.random(***REMOVED*** * Math.pow(16,4***REMOVED******REMOVED***.toString(16***REMOVED***;
    var z = Math.floor(Math.random(***REMOVED*** * Math.pow(16,4***REMOVED******REMOVED***.toString(16***REMOVED***;

    var file = '/tmp/' + [x,y,z].join('/'***REMOVED***;

    // should return the first dir created.
    // By this point, it would be profoundly surprising if /tmp didn't
    // already exist, since every other test makes things in there.
    // Note that this will throw on failure, which will fail the test.
    var made = mkdirp.sync(file***REMOVED***;
    t.equal(made, '/tmp/' + x***REMOVED***;

    // making the same file again should have no effect.
    made = mkdirp.sync(file***REMOVED***;
    t.equal(made, null***REMOVED***;
}***REMOVED***;
