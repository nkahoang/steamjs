function to_array(args***REMOVED*** {
    var len = args.length,
        arr = new Array(len***REMOVED***, i;

    for (i = 0; i < len; i += 1***REMOVED*** {
        arr[i] = args[i];
  ***REMOVED***

    return arr;
}

module.exports = to_array;
