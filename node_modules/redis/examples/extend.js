var redis = require("redis"***REMOVED***,
    client = redis.createClient(***REMOVED***;

// Extend the RedisClient prototype to add a custom method
// This one converts the results from "INFO" into a JavaScript Object

redis.RedisClient.prototype.parse_info = function (callback***REMOVED*** {
    this.info(function (err, res***REMOVED*** {
        var lines = res.toString(***REMOVED***.split("\r\n"***REMOVED***.sort(***REMOVED***;
        var obj = {};
        lines.forEach(function (line***REMOVED*** {
            var parts = line.split(':'***REMOVED***;
            if (parts[1]***REMOVED*** {
                obj[parts[0]] = parts[1];
          ***REMOVED***
      ***REMOVED******REMOVED***;
        callback(obj***REMOVED***
  ***REMOVED******REMOVED***;
};

client.parse_info(function (info***REMOVED*** {
    console.dir(info***REMOVED***;
    client.quit(***REMOVED***;
}***REMOVED***;
