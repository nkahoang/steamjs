'use strict';

var Node = require('./node'***REMOVED***;

/**
 * Initialize a new `Block` with an optional `node`.
 *
 * @param {Node} node
 * @api public
 */

var MixinBlock = module.exports = function MixinBlock(***REMOVED***{};

// Inherit from `Node`.
MixinBlock.prototype = Object.create(Node.prototype***REMOVED***;
MixinBlock.prototype.constructor = MixinBlock;

MixinBlock.prototype.type = 'MixinBlock';
