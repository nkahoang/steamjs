'use strict';

var Attrs = require('./attrs'***REMOVED***;

/**
 * Initialize a new `Mixin` with `name` and `block`.
 *
 * @param {String} name
 * @param {String} args
 * @param {Block} block
 * @api public
 */

var Mixin = module.exports = function Mixin(name, args, block, call***REMOVED***{
  Attrs.call(this***REMOVED***;
  this.name = name;
  this.args = args;
  this.block = block;
  this.call = call;
};

// Inherit from `Attrs`.
Mixin.prototype = Object.create(Attrs.prototype***REMOVED***;
Mixin.prototype.constructor = Mixin;

Mixin.prototype.type = 'Mixin';
