'use strict';

var Node = require('./node'***REMOVED***;

/**
 * Initialize a `Filter` node with the given
 * filter `name` and `block`.
 *
 * @param {String} name
 * @param {Block|Node} block
 * @api public
 */

var Filter = module.exports = function Filter(name, block, attrs***REMOVED*** {
  this.name = name;
  this.block = block;
  this.attrs = attrs;
};

// Inherit from `Node`.
Filter.prototype = Object.create(Node.prototype***REMOVED***;
Filter.prototype.constructor = Filter;

Filter.prototype.type = 'Filter';
