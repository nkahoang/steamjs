'use strict';

var Node = require('./node'***REMOVED***;

/**
 * Initialize a `Text` node with optional `line`.
 *
 * @param {String} line
 * @api public
 */

var Text = module.exports = function Text(line***REMOVED*** {
  this.val = line;
};

// Inherit from `Node`.
Text.prototype = Object.create(Node.prototype***REMOVED***;
Text.prototype.constructor = Text;

Text.prototype.type = 'Text';

/**
 * Flag as text.
 */

Text.prototype.isText = true;