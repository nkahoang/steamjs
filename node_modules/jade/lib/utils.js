'use strict';

/**
 * Merge `b` into `a`.
 *
 * @param {Object} a
 * @param {Object} b
 * @return {Object}
 * @api public
 */

exports.merge = function(a, b***REMOVED*** {
  for (var key in b***REMOVED*** a[key] = b[key];
  return a;
};

